# MyPlaneAgent 智能体架构深度分析报告

> 基于源代码的完整技术分析 | 生成时间: 2026-09-24

## 📋 项目概述

**MyPlaneAgent** 是一个独立的本地 AI 桌面应用，从 MyPlane 项目拆分而来。它基于 Electron + Vue 3 + TypeScript 构建，提供完整的本地 AI 工作台，包含模型管理、聊天、项目 Agent、工具执行和工作流编排能力。

### 基本信息
- **应用 ID**: `com.myplane.agent`
- **版本**: 0.1.0
- **技术栈**: Electron 41.10.7, Vue 3.5.42, TypeScript 5.9.3
- **Node 要求**: >= 22.13.0
- **默认 API 端口**: 8089
- **Agent 核心代码**: 约 3768 行 TypeScript

### 数据目录
- macOS: `~/Library/Application Support/myplane-agent`
- Windows: `%APPDATA%/myplane-agent`
- Linux: `~/.config/myplane-agent`
- 可通过 `MYPLANE_AGENT_DATA_DIR` 环境变量自定义

---

## 🏗️ 核心架构

### 1. 三层架构设计

```
┌─────────────────────────────────────────────┐
│         前端界面层 (Vue 3)                    │
│  - LocalAiStudio.vue (工作台主界面)          │
│  - LocalAiAgent.vue (Agent 执行界面)         │
│  - WorkflowDesigner.vue (工作流编排)         │
└─────────────────┬───────────────────────────┘
                  │ IPC 通信
┌─────────────────▼───────────────────────────┐
│      Electron 主进程 (调度与控制)             │
│  - 窗口管理与生命周期                         │
│  - 安全沙箱与权限控制                         │
│  - LocalAiStudioService (核心服务)          │
└─────────────────┬───────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
┌───────▼──────┐   ┌────────▼────────┐
│  模型推理层   │   │   工具执行层     │
│ - llama.cpp  │   │ - Python 工具   │
│ - 外部 API   │   │ - MCP 协议      │
│ - DeepSeek   │   │ - 系统命令      │
└──────────────┘   └─────────────────┘
```

### 2. 目录结构

```
MyPlaneAgent/
├── electron/
│   ├── main/
│   │   ├── index.ts              # 应用入口与窗口管理
│   │   ├── local-ai-studio.ts    # AI Studio 核心服务
│   │   ├── local-ai*.ts          # 模型、推理、聊天 API
│   │   └── agent/                # Agent 执行引擎
│   │       ├── execution-sandbox.ts    # 容器沙箱
│   │       ├── execution-supervisor.ts # 命令执行监控
│   │       ├── execution-journal.ts    # 执行记录管理
│   │       ├── workspace.ts           # 工作区工具
│   │       ├── tools.ts              # 工具注册与调用
│   │       ├── mcp.ts                # MCP 协议支持
│   │       ├── python.ts             # Python 工具执行
│   │       ├── workflow.ts           # 工作流引擎
│   │       └── automation.ts         # 定时任务
│   ├── preload/
│   │   └── index.cts             # 沙箱预加载桥
│   └── shared/
│       └── *.ts                  # 类型定义与共享逻辑
├── src/
│   ├── LocalAiStudio.vue         # 主工作台
│   ├── main.ts                   # 前端入口
│   └── local-ai/                 # AI 相关组件
│       ├── LocalAiAgent.vue      # Agent 界面
│       ├── WorkflowDesigner.vue  # 工作流设计器
│       └── *.vue                 # 其他组件
├── python/
│   └── *.py                      # Python 工具脚本
├── tests/
│   └── *.test.mjs               # 自动化测试
└── docs/
    └── *.md                     # 详细文档
```

---

## 🤖 Agent 执行引擎

### 核心特性

#### 1. **多模式支持**
- `chat`: 普通对话模式
- `coding`: 代码编写与调试
- `documents`: 文档处理
- `general`: 通用任务执行

#### 2. **执行策略**
- **策略模式** (`AgentPolicy`):
  - `read-only`: 只读模式
  - `confirm`: 逐次确认
  - `project-auto`: 项目级自动化

- **审批模式** (`AgentApprovalMode`):
  - `ask`: 每次询问
  - `auto`: 自动批准（受限）
  - `full`: 完全自动
  - `unrestricted`: 无限制

#### 3. **执行状态管理**
```typescript
type AgentStatus = 
  | 'running'    // 执行中
  | 'waiting'    // 等待用户确认
  | 'completed'  // 已完成
  | 'stopped'    // 已停止
  | 'failed'     // 执行失败
```

#### 4. **执行记录系统**

存储在 `executions/<taskId>/<eventId>.json`，包含：
- 工具来源与版本
- 参数指纹
- 目标文件哈希
- 验证结果
- 执行状态

**状态流转**:
```
prepared → running → verifying → succeeded/failed/unknown/not-applied
```

**中断恢复机制**:
- 全部匹配目标哈希 → 已完成
- 全部匹配基线 → 可重新规划
- 部分匹配 → 保留 unknown 状态，需用户核对

---

## 🛠️ 工具系统架构（基于源码分析）

### 1. 内置工具分类（agentTools + agentContextTools）

#### **上下文感知工具** (agentContextTools)
这些工具可访问任务级存储和历史：

- `inspect_build`: 只读发现 Node/Python 构建配置、包管理器、可执行动作和版本指纹
- `build_project`: 执行构建或测试动作，优先使用容器沙盒，返回结构化失败分析
- `reconcile_execution`: 核对中断步骤的文件哈希（只核对，不重放）
- `read_tool_result`: 按 resultId 和 offset 读取任务保存的完整工具结果
- `web_search`: 多引擎检索公开资料，聚合去重
- `web_fetch`: 读取公网 HTTP/HTTPS 页面正文，支持反爬降级

#### **工作区工具** (agentTools - 50+ 工具)

**动态加载机制** (`load_tool_pack`):
- 专业包: `web`, `code`, `tests`, `git`, `dependencies`, `runtime`, `images`, `archives`, `compare`, `todos`, `documents`, `edit`
- `catalog`: 分页列出工具名称（支持 offset）
- 按需加载，减少 Token 消耗

**Git 集成**:
```typescript
git_status, git_diff, git_log, git_show, git_blame
```

**代码分析**:
```typescript
inspect_project    // 一次识别语言、框架、入口、脚本、依赖
code_outline       // 提取类、函数、方法、类型和导出结构
find_symbol        // 按名称定位定义，返回文件、行号和代码片段
find_references    // 按标识符查找引用
find_todos         // 查找 TODO、FIXME、HACK、XXX 标记
```

**文件操作**:
```typescript
list_files         // 列出工作区文件，跳过依赖、Git、构建目录
search_files       // 字面文本检索，忽略大小写
read_file          // 分行读取，每次最多 250 行
file_info          // 读取元信息（大小、修改时间、SHA-256）
compare_files      // 生成统一差异
```

**文档处理**:
```typescript
read_document      // 提取 DOCX/PDF/XLSX/Markdown/TXT
                   // 支持分页（nextOffset, startPage）
                   // 保留 expectedHash 核对版本
create_document    // 生成 DOCX/Markdown/TXT（Markdown 格式正文）
create_spreadsheet // 创建 XLSX，每个表第一行为表头
```

**文本编辑**:
```typescript
write_file         // 创建或完整替换纯文本/代码
replace_text       // 唯一片段精确替换
apply_patch        // 多文件补丁（1-20 个文件，支持回退）
```

**测试与诊断**:
```typescript
get_diagnostics    // 类型检查或 Python 编译检查
run_test_case      // 精确运行测试文件或按名称筛选
process_status     // 查询进程和监听端口
http_request       // 向 localhost API 发送受限请求
```

**其他工具**:
```typescript
dependency_report  // 读取依赖清单、脚本和锁文件状态
archive_inspect    // 列出 ZIP/JAR/WHL/DOCX/XLSX/TAR 内容
image_ocr          // OCR 提取图片文字
run_command        // 在工作目录执行命令（优先沙箱）
set_plan           // 创建或更新任务计划（最多 12 步）
read_history       // 读取原始历史（支持字面文字筛选和分页）
```

#### **只读工具集** (readTools)
包含 21 个不修改文件的工具：
```typescript
const readTools = new Set([
  'load_tool_pack', 'git_status', 'git_diff', 'git_log', 'git_show', 
  'git_blame', 'inspect_project', 'code_outline', 'find_symbol', 
  'find_references', 'find_todos', 'dependency_report', 'file_info', 
  'compare_files', 'archive_inspect', 'process_status', 'image_ocr', 
  'list_files', 'search_files', 'read_file', 'read_document'
])
```

### 2. Python 工具执行架构

#### **PythonToolRuntime 类**
```typescript
class PythonToolRuntime {
  private workers = new Set<PythonWorker>()  // 工作进程池
  private metadata: PythonWorker             // 元数据工作器
  
  async execute(request, signal, timeoutMs, onOutput) {
    // 最多 8 个并发进程
    if (this.workers.size >= 8) throw new Error('执行队列已满')
    
    // 每次调用创建独立 Worker
    const worker = new PythonWorker(this.workerFile)
    this.workers.add(worker)
    try {
      return await worker.execute(request, signal, timeoutMs, onOutput)
    } finally {
      await worker.dispose()  // 执行后立即清理
      this.workers.delete(worker)
    }
  }
}
```

#### **PythonWorker 类 - 单工作器架构**
```typescript
class PythonWorker {
  private child?: ChildProcessWithoutNullStreams
  private pending = new Map<string, Pending>()  // 待处理请求
  
  // 进程通信协议
  // stdin: JSON 请求 + '\n'
  // stdout: JSON 响应 + '\n' (每行一个响应)
  // stderr: 错误日志（保留最后 20000 字符）
  
  private start(revision: string) {
    const executable = process.env.MYPLANE_PYTHON || 
                      (process.platform === 'win32' ? 'python' : 'python3')
    const child = spawn(executable, [this.script(), '--server'], {
      windowsHide: true,
      shell: false,
      env: {
        PYTHONIOENCODING: 'utf-8',
        PYTHONUTF8: '1',
        // 仅传递必要环境变量
      }
    })
    
    // 流式解析输出（StringDecoder 处理 UTF-8）
    // 限制：stdout 最多 1MB，stderr 保留 20KB
  }
}
```

#### **关键特性**
- **进程级隔离**: 每次调用独立进程，不共享状态
- **并发控制**: 最多同时 8 个进程
- **热重载**: 检测脚本文件变化，自动重启
- **超时管理**: 默认 60 秒，可配置
- **输出限制**: stdout 1MB，防止内存溢出
- **UTF-8 保证**: `PYTHONIOENCODING=utf-8`, `PYTHONUTF8=1`

#### **脚本位置查找优先级**
1. `workerFile` 参数（构造函数传入）
2. `MYPLANE_AGENT_TOOLS_PY` 环境变量
3. 打包后资源路径: `process.resourcesPath/python/agent_tools_worker.py`
4. 开发路径: `../../../python/agent_tools_worker.py`

### 3. MCP 协议集成（AgentMcpManager）

#### **核心架构**
```typescript
class AgentMcpManager {
  private running = new Map<string, Live>()  // 活动连接
  private connecting = new Set<string>()     // 连接中标记
  
  // Live 连接状态
  type Live = {
    revision: string              // 连接标识
    client: Client                // MCP 客户端
    transport: Transport          // stdio 或 http
    tools: Tool[]                 // 已发现工具
    status: 'connecting' | 'connected' | 'disconnected' | 'error'
    error?: string
    identity?: string             // 服务端版本
    controller: AbortController   // 取消控制
  }
}
```

#### **传输层支持**
```typescript
// Stdio 传输（本地进程）
new StdioClientTransport({
  command: config.command,
  args: config.args,
  env: secret.env || {},
  cwd: project.workspace,
  stderr: 'pipe',
  maxBufferSize: 1000000  // 1MB 缓冲
})

// HTTP 传输（远程服务）
new StreamableHTTPClientTransport(new URL(config.url), {
  requestInit: {
    redirect: 'error',
    headers: { Authorization: `Bearer ${secret.token}` }
  },
  reconnectionOptions: {
    maxRetries: 0,  // 不自动重连
    maxReconnectionDelay: 1000
  },
  fetch: async (url, init) => {
    // 跨域检查
    if (new URL(url).origin !== new URL(config.url).origin)
      throw new Error('禁止跨域 MCP 请求')
    
    // 响应大小限制（1MB）
    const response = await fetch(url, {...init, redirect: 'error'})
    if (Number(response.headers.get('content-length')) > 1000000)
      throw new Error('MCP 响应过大')
    
    // 流式限制
    // ...
  }
})
```

#### **版本协商**
```typescript
const client = new Client(
  { name: 'MyPlane', version: '0.2.3' },
  {
    capabilities: {},
    versionNegotiation: {
      mode: config.protocol === 'modern' 
        ? { pin: '2026-07-28' }  // 固定版本
        : 'legacy'                // 遗留协商
    },
    inputRequired: { autoFulfill: false }
  }
)
```

#### **连接流程**
```typescript
async connect(id: string) {
  // 1. 防止重复连接
  if (this.connecting.has(id)) throw new Error('服务正在连接')
  this.connecting.add(id)
  
  try {
    // 2. 断开旧连接
    await this.disconnect(id)
    
    // 3. 创建客户端和传输层
    const client = new Client(...)
    const transport = config.transport === 'stdio' 
      ? new StdioClientTransport(...)
      : new StreamableHTTPClientTransport(...)
    
    // 4. 设置事件处理
    client.onclose = () => { /* 更新状态 */ }
    client.onerror = () => { /* 记录错误 */ }
    client.setNotificationHandler('notifications/tools/list_changed', 
      () => { /* 要求重新核对 */ }
    )
    
    // 5. 连接超时控制（20秒）
    const deadline = setTimeout(() => 
      live.controller.abort(new Error('MCP 连接超时')), 20000
    )
    
    // 6. 执行连接和工具发现
    await client.connect(transport, { timeout: 15000, signal })
    const result = await client.listTools({}, { 
      timeout: 15000, 
      signal, 
      cacheMode: 'bypass' 
    })
    
    // 7. 验证工具列表
    if (result.tools.length > 100 || 
        JSON.stringify(result.tools).length > 200000)
      throw new Error('服务工具列表过大')
    
    // 8. 更新状态
    live.tools = result.tools
    live.status = 'connected'
    
    return this.view(config)
  } catch (error) {
    // 清理资源
    if (transport instanceof StdioClientTransport && transport.pid)
      await stopProcessTree(transport.pid)
    await client.close().catch(() => {})
    
    live.status = 'error'
    throw error
  } finally {
    clearTimeout(deadline)
    this.connecting.delete(id)
  }
}
```

#### **工具调用流程**
```typescript
specs(projectId: string): ToolSpec[] {
  return this.saved()
    .filter(config => config.projectId === projectId)
    .flatMap(config => {
      const live = this.running.get(config.id)
      if (live?.status !== 'connected') return []
      
      return live.tools
        .filter(tool => 
          // 检查工具已启用且 Schema 未变化
          config.enabledTools?.includes(tool.name) &&
          config.approvedSchemas?.[tool.name] === fingerprint(tool)
        )
        .map(tool => ({
          definition: {
            type: 'function',
            function: {
              // 生成唯一工具名称
              name: `mcp_${config.id.replace(/-/g, '').slice(0, 12)}_${fingerprint(tool.name).slice(0, 12)}`,
              description: `${config.name}: ${tool.name} — ${tool.description || ''}`.slice(0, 1500),
              parameters: tool.inputSchema
            }
          },
          revision: live.revision,
          source: `mcp:${config.id}`,
          risk: 'high',
          timeoutMs: 60000,
          
          execute: async (args, signal) => {
            // 执行前再次验证
            const current = this.config(config.id)
            if (live.status !== 'connected' ||
                !current.enabledTools?.includes(tool.name) ||
                current.approvedSchemas?.[tool.name] !== fingerprint(tool))
              throw new ToolError('MCP_CHANGED', '服务或工具权限已变化')
            
            // 再次发现工具（确保 Schema 未变）
            const latest = await live.client.listTools({}, {
              signal,
              timeout: 10000,
              cacheMode: 'bypass'
            })
            if (!latest.tools.some(current => 
                fingerprint(current) === fingerprint(tool)))
              throw new ToolError('MCP_CHANGED', '工具定义发生变化')
            
            // 调用工具
            const result = await live.client.callTool({
              name: tool.name,
              arguments: args
            }, { signal, timeout: 60000 })
            
            // 格式化结果
            const text = JSON.stringify({
              isError: result.isError,
              content: result.content.map(item =>
                item.type === 'text'
                  ? { type: 'text', text: item.text }
                  : { type: item.type, note: '非文本结果未自动下载或执行' }
              ),
              structuredContent: result.structuredContent
            })
            
            if (result.isError)
              throw new ToolError('MCP_TOOL_ERROR', text.slice(0, 1000), {
                result: JSON.parse(text)
              })
            
            return text
          }
        }))
    })
}
```

#### **安全特性**
- **凭据加密**: 使用 `safeStorage` 加密 env 和 token
- **跨域保护**: HTTP 传输禁止跨域请求
- **响应限制**: 单次响应最多 1MB
- **工具数量**: 最多 100 个工具，JSON 最多 200KB
- **Schema 指纹**: 使用 `fingerprint()` 验证工具定义未变化
- **启用控制**: 最多启用 20 个工具
- **断线处理**: 不自动重连，不重放调用
- **进程清理**: Stdio 传输时清理进程树

### 4. 工具选择策略（Token 预算管理）

#### **Schema Token 预算分配**
```typescript
// 为系统提示、用户输入和输出预留空间
const systemPromptBudget = 2000    // 系统提示
const userInputBudget = 4000       // 用户输入
const outputBudget = maxTokens * 0.2  // 输出预留 20%

const toolBudget = contextLength 
  - systemPromptBudget 
  - userInputBudget 
  - outputBudget
  - recentMessages  // 最近消息
```

#### **工具加载优先级**
1. **已加载工具**: 上一轮使用的工具优先进入下一轮
2. **按 Token 预算选择**: 累计 Schema Token 直到达到预算
3. **应用数量上限**: 即使有预算，也不超过配置的最大工具数
4. **过大 Schema**: 单个 Schema 超过预算时，仍可能被选中（必要工具）

#### **动态加载机制**
```typescript
// load_tool_pack 工具
{
  pack: string,  // 包名或工具名
  offset: number // catalog 分页偏移
}

// 专业包映射
const packs = {
  web: ['web_search', 'web_fetch'],
  code: ['code_outline', 'find_symbol', 'find_references'],
  tests: ['get_diagnostics', 'run_test_case'],
  git: ['git_status', 'git_diff', 'git_log', 'git_show', 'git_blame'],
  // ...
  edit: ['replace_text'],
  catalog: [] // 特殊：返回工具名称列表
}
```

---

## 🔒 安全与权限控制（基于源码实现）

### 1. 容器沙箱机制（execution-sandbox.ts）

#### **沙箱能力检测**
```typescript
function sandboxCapability(force = false, imageOverride?: string): SandboxCapability {
  const image = configuredImage(imageOverride)  // 默认: node:22-bookworm-slim
  const disabled = process.env.MYPLANE_SANDBOX_DISABLE === '1'
  const preferred = process.env.MYPLANE_SANDBOX_RUNTIME?.trim()
  
  // 缓存 30 秒
  if (!force && cached?.key === key && Date.now() - cached.at < 30_000)
    return cached.value
  
  if (disabled) return { available: false, reason: '容器沙盒已由环境配置禁用' }
  
  // 优先级: 环境变量指定 > docker > podman
  const candidates = (preferred ? [preferred] : ['docker', 'podman'])
    .filter((name, index, all) => 
      /^(docker|podman)$/.test(name) && all.indexOf(name) === index
    )
  
  for (const runtime of candidates) {
    // 检查运行时是否可用（2.5 秒超时）
    const version = spawnSync(runtime, ['version'], {
      encoding: 'utf8',
      stdio: 'ignore',
      timeout: 2500,
      windowsHide: true
    })
    if (version.status !== 0) continue
    
    // 检查镜像是否已存在（5 秒超时）
    const inspected = spawnSync(runtime, ['image', 'inspect', image], {
      encoding: 'utf8',
      stdio: 'ignore',
      timeout: 5000,
      windowsHide: true
    })
    
    if (inspected.status === 0) {
      return {
        available: true,
        runtime,
        image,
        reason: `使用本机已有 ${runtime} 镜像 ${image}`
      }
    }
  }
  
  return {
    available: false,
    image,
    reason: `未发现可用的 Docker/Podman 运行时及本地镜像 ${image}；不会自动拉取镜像`
  }
}
```

#### **沙箱请求构建**
```typescript
function buildSandboxedRequest(
  request: { executable: string; args?: string[]; shell?: boolean; cwd: string },
  capability: SandboxCapability
) {
  if (!capability.available || !capability.runtime)
    throw new Error('容器沙盒不可用')
  
  const workspace = path.resolve(request.cwd)
  const command = request.shell === true
    ? request.executable
    : [request.executable, ...request.args || []].map(quote).join(' ')
  
  const args = [
    'run',
    '--rm',               // 容器退出后自动删除
    '--init',             // 使用 init 进程管理僵尸进程
    '--network', 'none',  // 完全断网
    '--read-only',        // 只读根文件系统
    '--cap-drop', 'ALL',  // 移除所有 Linux capabilities
    '--security-opt', 'no-new-privileges',  // 禁止提权
    '--pids-limit', '256',     // 进程数限制
    '--memory', '2g',          // 内存限制 2GB
    '--cpus', '2',             // CPU 限制 2 核
    
    // 临时文件系统（可写）
    '--tmpfs', '/tmp:rw,nosuid,nodev,size=512m,mode=1777',
    '--tmpfs', '/home/agent:rw,nosuid,nodev,size=64m,mode=1777',
    
    // 环境变量
    '-e', 'HOME=/home/agent',
    '-e', 'CI=1',
    
    // 挂载工作目录（可写）
    '-v', `${workspace}:/workspace:rw`,
    '-w', '/workspace'
  ]
  
  // 用户映射（非 root）
  const user = typeof process.getuid === 'function' && typeof process.getgid === 'function'
    ? `${process.getuid()}:${process.getgid()}`
    : '65532:65532'  // nobody:nogroup
  args.push('--user', user)
  
  // 镜像和命令
  args.push(capability.image, '/bin/sh', '-lc', command)
  
  return {
    executable: capability.runtime,  // docker 或 podman
    args,
    cwd: workspace,
    shell: false
  }
}
```

#### **沙箱元数据**
```typescript
type SandboxMetadata = {
  active: boolean              // 是否实际使用沙箱
  runtime?: 'docker' | 'podman'
  image?: string
  network: 'none' | 'host'     // 网络隔离状态
  home: 'temporary'            // 临时 HOME 目录
  reason: string               // 状态原因
}
```

**关键限制**:
- 网络: 完全断网（`--network none`）
- 文件系统: 根文件系统只读，仅 `/tmp` 和 `/home/agent` 可写
- 进程: 最多 256 个
- 内存: 2GB
- CPU: 2 核
- 用户: 非 root（映射宿主机用户或 nobody）
- 安全: 移除所有 capabilities，禁止提权

### 2. 进程执行监控（execution-supervisor.ts）

#### **executeProcess 函数**
```typescript
async function executeProcess(
  request: ProcessRequest,
  signal: AbortSignal,
  onOutput?: (text: string) => void
): Promise<ProcessResult> {
  signal.throwIfAborted()
  
  // 1. 沙箱能力检测
  const capability = request.sandbox 
    ? sandboxCapability(false, request.sandboxImage)
    : { available: false, image: '', reason: '此命令未请求容器沙盒' }
  
  const active = Boolean(request.sandbox && capability.available)
  
  // 2. 沙箱强制模式检查
  if (request.sandbox === 'required' && !active)
    throw new ToolError('SANDBOX_UNAVAILABLE', capability.reason)
  
  // 3. 构建启动请求（沙箱或直接）
  const launched = active 
    ? buildSandboxedRequest(request, capability)
    : request
  
  // 4. 创建临时 HOME 目录
  const temporaryHome = fs.mkdtempSync(
    path.join(os.tmpdir(), 'myplane-agent-home-')
  )
  
  // 5. 构建环境变量（最小化）
  const env: NodeJS.ProcessEnv = {}
  for (const key of ['PATH', 'SystemRoot', 'ComSpec', 'PATHEXT', 
                      'TEMP', 'TMP', 'TMPDIR', 'LANG', 'LC_ALL']) {
    if (process.env[key]) env[key] = process.env[key]
  }
  
  // HOME 和 XDG 目录（沙箱模式使用宿主机环境，否则使用临时目录）
  env.HOME = active && process.env.HOME 
    ? process.env.HOME 
    : temporaryHome
  env.USERPROFILE = active && process.env.USERPROFILE 
    ? process.env.USERPROFILE 
    : temporaryHome
  env.XDG_CONFIG_HOME = active && process.env.XDG_CONFIG_HOME 
    ? process.env.XDG_CONFIG_HOME 
    : path.join(temporaryHome, '.config')
  env.XDG_CACHE_HOME = path.join(temporaryHome, '.cache')
  
  return new Promise((resolve, reject) => {
    const started = Date.now()
    const limit = request.maxOutputBytes ?? 2_000_000  // 默认 2MB
    
    // 6. 启动进程
    const child = spawn(launched.executable, launched.args || [], {
      cwd: launched.cwd,
      shell: launched.shell === true,
      env,
      detached: process.platform !== 'win32',  // Unix: 独立进程组
      windowsHide: true,
      stdio: ['ignore', 'pipe', 'pipe']
    })
    
    let output = ''
    let bytes = 0
    let truncated = false
    let termination: 'cancelled' | 'timeout' | 'output-limit' | undefined
    
    // 7. 停止函数（进程树终止）
    const stop = () => cleanup ?? (async () => {
      if (!child.pid) return
      
      // Unix: 使用进程组 SIGKILL
      if (process.platform !== 'win32') {
        try {
          process.kill(-child.pid, 'SIGKILL')
          return
        } catch {
          // 进程组不可用，回退到进程树
        }
      }
      
      // Windows 或进程组失败: 停止进程树
      await stopProcessTree(child.pid)
      try {
        child.kill('SIGKILL')
      } catch {
        // Already exited
      }
    })()
    
    // 8. 超时控制
    const timer = setTimeout(() => {
      termination = 'timeout'
      void stop()
    }, request.timeoutMs)
    
    // 9. 取消监听
    const abort = () => {
      termination = 'cancelled'
      void stop()
    }
    signal.addEventListener('abort', abort, { once: true })
    if (signal.aborted) abort()
    
    // 10. UTF-8 解码（分别处理 stdout 和 stderr）
    const decoders = [new StringDecoder('utf8'), new StringDecoder('utf8')]
    
    const append = (chunk: Buffer, index: number) => {
      const available = Math.max(0, limit - bytes)
      bytes += chunk.length
      output += decoders[index].write(chunk.subarray(0, available))
      onOutput?.(output)
      
      // 输出限制检查
      if (bytes > limit) {
        truncated = true
        termination ??= 'output-limit'
        void stop()
      }
    }
    
    child.stdout.on('data', chunk => append(chunk, 0))
    child.stderr.on('data', chunk => append(chunk, 1))
    
    // 11. 完成处理
    const finish = async (error?: Error, code: number | null = null) => {
      if (settled) return
      settled = true
      clearTimeout(timer)
      signal.removeEventListener('abort', abort)
      
      if (cleanup) await cleanup
      
      // 刷新解码器剩余内容
      output += decoders.map(decoder => decoder.end()).join('')
      
      // 清理临时目录
      try {
        fs.rmSync(temporaryHome, { recursive: true, force: true })
      } catch {
        // Best-effort cleanup
      }
      
      if (error) {
        reject(spawned 
          ? error 
          : new ToolError('PROCESS_START_FAILED', 
                          '无法启动执行环境：' + error.message)
        )
        return
      }
      
      resolve({
        exitCode: code,
        durationMs: Date.now() - started,
        output,
        truncated,
        sandbox: sandboxMetadata(capability, active),
        ...(termination ? {
          termination,
          error: termination === 'cancelled' 
            ? '任务已停止'
            : termination === 'timeout'
            ? '命令超过运行时间限制，已请求终止进程树'
            : '命令输出超过容量，已请求终止进程树'
        } : {})
      })
    }
    
    child.on('error', error => { void finish(error) })
    child.on('close', code => { void finish(undefined, code) })
  })
}
```

**关键特性**:
- **UTF-8 保证**: 使用 `StringDecoder` 正确处理多字节字符
- **输出限制**: 默认 2MB，超出自动终止
- **超时控制**: 可配置，超时自动终止
- **进程树清理**: 
  - Unix: 优先使用进程组 SIGKILL（`-pid`）
  - Windows: 调用 `stopProcessTree()` 递归终止
- **临时环境**: 每次执行创建独立临时 HOME
- **最小环境**: 仅传递必要环境变量
- **取消支持**: 通过 `AbortSignal` 取消

### 3. 文件权限控制（workspace.ts）

#### **AgentWorkspace 类**
```typescript
class AgentWorkspace {
  readonly root: string
  
  constructor(root: string) {
    this.root = fs.realpathSync(root)  // 解析真实路径
    if (!fs.statSync(this.root).isDirectory())
      throw new Error('工作目录不可用')
  }
  
  resolve(value: unknown, allowMissing = false) {
    const relative = bounded(value, '工作区相对路径', 2000)
    
    // 1. 路径格式检查
    if (path.isAbsolute(relative) ||
        relative.includes('\\') ||
        relative.includes('\0') ||
        relative.includes(':'))
      throw new Error('请使用工作区内的相对路径')
    
    // 2. 路径分段
    const parts = relative.split('/').filter(p => p && p !== '.')
    
    // 3. 逐段检查
    if (parts.some(p =>
      p === '..' ||                                    // 禁止向上
      /[<>:"|?*\x00-\x1f]/.test(p) ||                 // 禁止特殊字符
      /[. ]$/.test(p) ||                               // 禁止末尾空格/点
      /^(con|prn|aux|nul|com[1-9]|lpt[1-9])(?:\.|$)/i.test(p) ||  // Windows 保留名
      ignored.has(p.toLowerCase()) ||                  // 依赖/Git 目录
      secret(p)                                        // 密钥文件
    )) {
      throw new Error('此路径不允许访问（越界、依赖目录或敏感文件）')
    }
    
    // 4. 工作目录稳定性检查
    if (fs.realpathSync(this.root) !== this.root)
      throw new Error('工作目录已发生变化，请重新选择')
    
    // 5. 逐级构建并检查
    let current = this.root
    for (const part of parts) {
      current = path.join(current, part)
      try {
        const st = fs.lstatSync(current)
        
        // 禁止符号链接
        if (st.isSymbolicLink())
          throw new Error('不允许访问符号链接')
        
        // 禁止硬链接（多链接文件）
        if (st.isFile() && st.nlink > 1)
          throw new Error('不允许访问具有多个硬链接的文件')
      } catch (e) {
        if ((e as NodeJS.ErrnoException).code === 'ENOENT' && allowMissing)
          continue
        throw e
      }
    }
    
    return current
  }
}
```

#### **保护机制**
```typescript
// 忽略目录
const ignored = new Set([
  '.git', 'node_modules', 'dist', 'dist-electron', 
  'release', 'vendor', '.idea', '.venv', 'venv', '__pycache__'
])

// 密钥文件检测
const secret = (name: string) =>
  /^\.env(?:\.|$)/i.test(name) && 
  !/^\.env\.(example|sample|template)$/i.test(name) ||  // .env* 但非示例
  /^(\.ssh|\.aws|\.gnupg|credentials(?:\.json)?|id_rsa|id_ed25519)$/i.test(name) ||  // 凭据目录/文件
  /\.(pem|key|p12|pfx)$/i.test(name)  // 证书/密钥文件

// 文件大小限制
const maxFile = 2 * 1024 * 1024  // 2MB

// 二进制文档检测
const binaryDocument = /\.(docx?|xlsx?|pptx?|pdf|odt|ods|odp|rtf|zip|7z|rar|png|jpe?g|gif|webp|bmp|tiff?)$/i
```

### 4. 写入操作保护

#### **文件写入流程**
```typescript
async prepare(name: string, args: Record<string, unknown>, signal: AbortSignal): Promise<PreparedAction> {
  signal.throwIfAborted()
  
  // 1. 解析目标路径
  const relative = bounded(args.path, '目标路径', 2000)
  const target = this.resolve(relative, true)  // 允许不存在
  
  if (target === this.root)
    throw new Error('目标必须是文件')
  
  // 2. 读取现有文件
  let previous: Buffer | undefined
  let mode = 0o644
  if (fs.existsSync(target)) {
    const st = fs.statSync(target)
    if (!st.isFile() || st.size > 20 * 1024 * 1024)
      throw new Error('目标不是文件或超过 20 MB')
    previous = fs.readFileSync(target)
    mode = st.mode & 0o777
  }
  
  // 3. 计算新内容
  let next: Buffer
  // ... 根据工具类型处理 ...
  
  // 4. 计算哈希
  const expected = previous ? digest(previous) : null
  
  // 5. 返回准备好的操作
  return {
    expectedFiles: [{
      path: relative,
      beforeHash: expected,
      afterHash: digest(next)
    }],
    preview: {
      path: relative,
      before: previous?.toString('utf8'),
      after: next.toString('utf8'),
      note: previous 
        ? '将替换现有文件；确认时会重新检查文件版本。'
        : '将创建新文件。'
    },
    artifact: { path: relative, kind: 'file' },
    
    execute: async (signal) => {
      signal.throwIfAborted()
      const file = this.resolve(relative, true)
      
      // 6. 再次检查文件大小（防止确认期间变化）
      if (fs.existsSync(file) && fs.statSync(file).size > 20 * 1024 * 1024)
        throw new Error('文件在确认期间已变大，本次操作未执行')
      
      // 7. 版本校验
      const current = fs.existsSync(file) 
        ? fs.readFileSync(file) 
        : undefined
      if ((current ? digest(current) : null) !== expected)
        throw new Error('文件在确认期间已被修改，本次操作未执行。请重新读取并生成修改。')
      
      // 8. 原子写入（临时文件 + 重命名）
      fs.mkdirSync(path.dirname(file), { recursive: true })
      this.resolve(relative, true)
      
      const temp = path.join(
        path.dirname(file),
        `.myplane-agent-${randomUUID()}.tmp`
      )
      
      try {
        fs.writeFileSync(temp, next, { flag: 'wx', mode })
        signal.throwIfAborted()
        fs.renameSync(temp, file)
      } finally {
        if (fs.existsSync(temp)) fs.unlinkSync(temp)
      }
      
      // 9. 文档验证（DOCX/XLSX）
      let validation: any
      if (kind !== 'file') {
        try {
          validation = JSON.parse(await extractDocument(file, {}, signal))
        } catch {
          throw new ToolError('RESULT_UNKNOWN', 
            '文件已保存，但重新读取验证未完成；请先检查 ' + relative + '，不要重复覆盖。'
          )
        }
      }
      
      return JSON.stringify({
        path: relative,
        bytes: next.length,
        status: 'saved',
        ...(validation ? {
          validation: {
            readable: true,
            characters: validation.totalCharacters,
            note: '已重新打开并提取内容；未验证视觉排版。'
          }
        } : {})
      })
    }
  }
}
```

#### **多文件补丁保护**
```typescript
async patch(args: Record<string, unknown>, signal: AbortSignal): Promise<PreparedAction> {
  if (!Array.isArray(args.changes) || 
      !args.changes.length || 
      args.changes.length > 20)
    throw new Error('补丁需要 1–20 个文件')
  
  const seen = new Set<string>()
  const changes: Array<{
    path: string
    before?: string
    after: string
    expected: string | null
    mode: number
  }> = []
  
  // 1. 验证所有文件（执行前）
  for (const value of args.changes) {
    const change = object(value)
    const name = bounded(change.path, '补丁路径', 2000)
    const file = this.resolve(name, true)
    
    // 二进制文档检查
    if (binaryDocument.test(name))
      throw new Error('文本补丁不能写入二进制文档')
    
    // 重复路径检查（大小写敏感）
    const identity = process.platform === 'win32' 
      ? file.toLowerCase() 
      : file
    if (seen.has(identity))
      throw new Error('同一文件只能出现一次')
    seen.add(identity)
    
    // 基线匹配检查
    const before = fs.existsSync(file) ? this.read(name) : undefined
    if (before === undefined && change.before !== undefined ||
        before !== undefined && change.before !== before)
      throw new Error('补丁基线不匹配，请重新读取 ' + name)
    
    if (typeof change.after !== 'string' || change.after.length > 100000)
      throw new Error('补丁内容过大或无效')
    
    changes.push({
      path: name,
      before,
      after: change.after,
      expected: before === undefined ? null : digest(Buffer.from(before)),
      mode: fs.existsSync(file) ? fs.statSync(file).mode & 0o777 : 0o644
    })
  }
  
  return {
    expectedFiles: changes.map(change => ({
      path: change.path,
      beforeHash: change.expected,
      afterHash: digest(Buffer.from(change.after))
    })),
    preview: {
      changes: changes.map(({ path, before, after }) => ({
        path,
        before,
        after
      })),
      note: '多文件补丁：写入前统一核对所有基线，失败时尝试回退本次写入。'
    },
    
    execute: async (s) => {
      signal.throwIfAborted()
      s.throwIfAborted()
      
      // 2. 再次验证所有基线（原子性检查）
      const valid = (change: typeof changes[number], after = false) => {
        const file = this.resolve(change.path, true)
        const value = fs.existsSync(file) 
          ? digest(Buffer.from(this.read(change.path))) 
          : null
        if (value !== (after ? digest(Buffer.from(change.after)) : change.expected))
          throw new Error('文件已变化：' + change.path)
        return file
      }
      
      changes.forEach(change => valid(change))
      
      const applied: number[] = []
      const pending: string[] = []
      
      try {
        // 3. 逐个写入
        for (let i = 0; i < changes.length; i++) {
          const change = changes[i]
          const file = this.resolve(change.path, true)
          
          // 临时文件 + 原子重命名
          const temp = file + '.' + randomUUID() + '.tmp'
          try {
            fs.writeFileSync(temp, change.after, { 
              flag: 'wx', 
              mode: change.mode 
            })
            signal.throwIfAborted()
            s.throwIfAborted()
            fs.renameSync(temp, file)
            applied.push(i)
          } finally {
            if (fs.existsSync(temp)) fs.unlinkSync(temp)
          }
        }
        
        // 4. 验证所有写入
        changes.forEach(change => valid(change, true))
        
        return JSON.stringify({
          status: 'applied',
          count: changes.length,
          paths: changes.map(c => c.path)
        })
      } catch (error) {
        // 5. 回退已写入文件
        const failed: string[] = []
        for (const i of applied.reverse()) {
          const change = changes[i]
          try {
            const file = this.resolve(change.path, true)
            
            // 检查当前内容是否是刚写入的
            if (change.before === undefined 
              ? fs.existsSync(file) 
              : this.read(change.path) !== change.before)
              throw new Error('changed')
            
            // 恢复原内容或删除
            fs.writeFileSync(file, change.after, {
              flag: change.before === undefined ? 'wx' : 'w'
            })
          } catch {
            failed.push(change.path)
          }
        }
        
        throw new Error(
          String(error) + 
          (failed.length 
            ? '；恢复未完成，需核对：' + failed.join(',')
            : '；已回退此次恢复'
          )
        )
      }
    }
  }
}
```

**关键保护**:
- **版本校验**: 确认前后两次检查文件哈希
- **原子写入**: 临时文件 + 重命名
- **基线匹配**: 补丁基线必须与当前文件完全匹配
- **自动回退**: 多文件补丁失败时尝试恢复（尽力而为）
- **重复检测**: 防止同一文件多次修改
- **大小限制**: 单文件最多 100KB，总体最多 20 文件

### 5. 自动写入策略

#### **canAutoWrite 判断**
```typescript
canAutoWrite(name: string, args: Record<string, unknown>, scopes: string[]): boolean {
  // 1. 仅写入工具允许自动执行
  if (!['write_file', 'replace_text', 'apply_patch', 
        'create_document', 'create_spreadsheet'].includes(name))
    return false
  
  // 2. 提取目标路径
  const names = typeof args.path === 'string' 
    ? [args.path] 
    : Array.isArray(args.changes)
    ? args.changes.map(item => (item as { path: string }).path)
    : []
  
  // 3. 所有路径必须在授权范围内
  return names.length > 0 && names.every(name => {
    try {
      this.resolve(name, true)
      
      // 4. 排除敏感文件
      if (/(^|\/)(\.[^/]+|package[^/]*\.json|[^/]*lock[^/]*|AGENTS\.md|[^/]*\.(sh|bat|cmd|ps1)|[^/]*\.config\.[^/]+|tsconfig[^/]*\.json|Makefile|Dockerfile)$/i.test(name))
        return false
      
      // 5. 检查是否在授权 scope 内
      return scopes.some(scope =>
        scope === '.' ||                            // 整个工作区
        name === scope ||                           // 精确匹配
        name.startsWith(scope.replace(/\/$/, '') + '/')  // 子目录
      )
    } catch {
      return false
    }
  })
}
```

**排除的敏感文件**:
- 隐藏文件: `.something`
- 包管理: `package*.json`, `*lock*`
- 配置文件: `*.config.*`, `tsconfig*.json`, `Makefile`, `Dockerfile`
- 脚本文件: `*.sh`, `*.bat`, `*.cmd`, `*.ps1`
- Agent 元数据: `AGENTS.md`

### 6. 审计与追踪

#### **AgentAudit 记录**
```typescript
type AgentAudit = {
  callId: string                // 调用唯一 ID
  source: string                // 工具来源（内置/Python/MCP）
  risk: string                  // 风险等级
  revision?: string             // 工具版本
  authorization?: 'automatic' | 'confirmed' | 'denied'
  startedAt: string             // 开始时间（ISO 8601）
  endedAt?: string              // 结束时间
  durationMs?: number           // 持续毫秒数
  errorCode?: string            // 错误代码
  exitCode?: number | null      // 进程退出码
}
```

#### **工具权限分级**
```typescript
type ToolSpec = {
  definition: ToolDefinition     // 工具定义（OpenAI 格式）
  source: string                 // 来源标识
  risk: 'read-only' | 'low' | 'high'  // 风险等级
  revision?: string              // 版本标识
  timeoutMs: number              // 超时时间
  destination?: string           // 目标（MCP 服务地址/命令）
  execute: (args, signal) => Promise<string>
}

// 风险分级规则
- read-only: readTools 集合中的工具
- high: 所有写入、命令、MCP 工具
- low: （预留）
```

**权限控制流程**:
1. 工具注册时标记风险等级
2. 执行前检查 `AgentPolicy` 和 `AgentApprovalMode`
3. `read-only` 工具直接执行
4. `high` 风险工具需确认（除非在自动范围内）
5. 每次执行记录审计日志
6. 内置工具实现修改需降级为 `high`（防止绕过）

---

## 🧠 上下文管理

### 1. 小上下文优化

**触发条件**: 容量 ≤ 8192 Tokens

**优化策略**:
- 简短系统提示
- 单步工具调用（一轮只执行一个工具）
- 输出预留约 20% 容量
- 多调用整批拒绝+格式纠错重试

### 2. 自动压缩

**压缩时机**:
- 上下文接近容量上限
- 保留最新要求
- 保留完整工具调用组
- 原始历史不删除

**失败恢复**:
从以下内容重建上下文：
- 全部原始用户文字
- 已有摘要
- 执行记录
- 标注恢复来源

**禁止降级情况**:
- 恢复载荷无法缩短
- 超容量
- 含无损图片
- 用户取消
- Token 预算耗尽

### 3. 大结果分页

**触发**: 工具返回超过结果预算

**处理**:
1. 完整返回值保存到 `results/<taskId>/<eventId>.txt`
2. 消息保留：JSON 片段 + 状态 + 结果 ID + 下一偏移
3. `read_tool_result` 分页读取
4. 重启后继续可用
5. 删除任务时一并删除

### 4. Token 用量统计

**数据来源**:
- `stream_options.include_usage`
- 读取 usage 帧
- 兼容 llama.cpp 的 timings 计数

**统计内容**:
- 输入 Token（含工具定义）
- 输出 Token（含思考 Token）
- 累计总量
- 单次用量（聊天）

**显示策略**:
- 窄窗口：仅总量
- 悬停：输入/输出明细
- 缺失标注：`*` 或"未返回"
- 不根据字数估算

---

## 🔄 工作流引擎

### 节点类型

1. **Agent 节点**: 执行 AI 任务
2. **确定性路由**: 条件分支
3. **数据节点**: 变量传递
4. **汇合节点**: 多路径合并
5. **人工确认**: 需用户介入
6. **系统通知**: 发送通知
7. **结束节点**: 流程终点

### 特性

- 可视化编排
- 节点运行时间线
- 变量引用与传递
- 模型选择（本地/远程）
- 独立编辑窗口

---

## ⏰ 定时任务

### 调度方式

1. **单次**: 指定时间执行一次
2. **间隔**: 固定间隔重复
3. **每日**: 每天固定时间
4. **每周**: 每周固定时间
5. **Cron**: 自定义 Cron 表达式

### 执行对象

- 项目 Agent
- 工作流

### 管理功能

- 创建/编辑/删除
- 启用/禁用
- 执行历史
- 失败重试

---

## 🌐 远程模型支持

### DeepSeek 云端 API

**配置路径**: 设置 → 服务连接 → DeepSeek 云端 API

**特性**:
- 预设地址: `https://api.deepseek.com`
- 默认模型: `deepseek-flash`
- 可选: `deepseek-v4-pro` 及其他
- 实时获取模型列表

**能力预设**:
- 输出 Token: 65536（可提升至 393216）
- 上下文预算: 1000000
- 聊天默认开启思考
- Agent 模式：
  - 快速: 8192 单轮预算
  - 深度: 完整工作区预算
  - 摘要: 关闭思考

**安全**:
- 系统安全存储加密 API Key
- 仅 Electron 主进程发送
- 更换地址需重新填写

### 其他 OpenAI 兼容服务

支持手动配置：
- 自定义服务地址
- 自定义模型 ID
- API Key 管理
- 上下文长度配置

---

## 📊 流式进度

### 实现机制

**流式工具调用**:
- 兼容 SSE 流式响应
- 兼容整段 JSON 响应

**进度显示**:
- 每轮等待时长
- 阶段识别: 思考 / 答复 / 工具调用
- 接收字符数统计

**超时控制**:
- 首个输出: 最多等待 10 分钟
- 空闲超时: 连续 5 分钟无新内容
- 单轮总时长: 最多 30 分钟
- 内置网关: 留出 1 分钟余量

**健壮性**:
- 工具调用必须接收完整结束标记
- 参数必须通过验证
- 超时/停止/截断不执行半成品

---

## 🧪 测试策略

### 自动化测试

**单元测试** (`tests/*.test.mjs`):
- Agent 执行: 29 项
- Token 统计: 3 项
- 小上下文: 覆盖 4K/8K 载荷
- 执行恢复: 中断、部分写入、回退
- 工具权限: 连续保存、历史修正

**集成测试**:
- 完整回归: 143 项通过、4 项按环境跳过
- 模拟模型服务验证
- 真实临时文件验证

### Electron 界面测试

**覆盖**:
- 原生目录授权
- 项目创建/取消/去重
- 项目分组/折叠
- 六轮工具执行
- 代码修改确认
- 命令验证
- Word 产物生成
- 任务恢复
- 响应式布局: 520/760/1280px
- 主题: 浅色/深色

### 测试命令

```bash
npm test                 # 单元测试
npm run test:smoke       # 冒烟测试
npm run test:history     # 历史恢复（需先启动 vite）
```

---

## 🔧 开发与构建

### 开发环境

```bash
npm ci                   # 安装依赖
npm run dev              # 启动开发服务器（127.0.0.1:5174）
npm run check            # 类型检查
```

### 生产构建

```bash
npm run build            # 当前平台
npm run build:win        # Windows (.exe)
npm run build:mac        # macOS (.dmg)
npm run build:linux      # Linux (.AppImage + .deb)
```

**产物目录**: `release/`

### 主要依赖

**运行时**:
- `@modelcontextprotocol/client`: MCP 协议客户端
- `element-plus`: UI 组件库
- `marked`: Markdown 解析
- `pdfjs-dist`: PDF 处理
- `exceljs`: Excel 处理
- `jszip`: ZIP 压缩
- `dompurify`: HTML 净化

**开发**:
- `electron-builder`: 应用打包
- `vite`: 前端构建
- `vue-tsc`: Vue 类型检查
- `concurrently`: 并发任务

---

## 🔄 模型交互与流式处理（model.ts）

### 1. 请求架构

#### **requestAgentModel 函数签名**
```typescript
async function requestAgentModel(
  connection: AgentConnection,
  model: string,
  messages: AgentMessage[],
  signal: AbortSignal,
  options: RequestOptions = {}
): Promise<AgentAnswer>

type AgentConnection = {
  endpoint: string
  key: string
  maxTokens: number
  contextLength: number
  localLlama?: boolean
  apiFormat?: RemoteApiFormat  // 'openai' | 'anthropic'
}

type RequestOptions = {
  onResponse?: (text: string) => void        // SSE 原始帧
  tools?: false | ToolDefinition[]           // 工具定义
  summary?: boolean                          // 是否摘要请求
  thinking?: boolean                         // 是否启用思考
  onContent?: (text: string) => void         // 内容流
  onReasoning?: (text: string) => void       // 思考流
  onUsage?: (usage: TokenUsage) => void      // Token 统计
  onProgress?: (progress) => void            // 进度更新
  timing?: Partial<typeof agentModelTiming>  // 超时配置
}
```

#### **超时控制**
```typescript
const agentModelTiming = {
  firstResponseMs: 10 * 60 * 1000,  // 首个输出: 10 分钟
  idleMs: 5 * 60 * 1000,            // 空闲超时: 5 分钟
  totalMs: 30 * 60 * 1000           // 单轮总时长: 30 分钟
}

// 动态超时逻辑
let idle = setTimeout(() => 
  abort(`等待模型首个输出超过 ${minutes(timing.firstResponseMs)}。`),
  timing.firstResponseMs
)

const activity = () => {
  clearTimeout(idle)
  idle = setTimeout(() => 
    abort(`模型连续 ${minutes(timing.idleMs)} 没有返回新内容。`),
    timing.idleMs
  )
}

// 每次有新内容时调用 activity() 续期
```

### 2. 流式解析架构

#### **SSE 流处理**
```typescript
for await (const frame of sseData(response.body, combined)) {
  options.onResponse?.(frame + '\n')  // 原始帧回调
  
  if (frame === '[DONE]') break
  
  wireSize += frame.length
  if (wireSize > responseWireLimit)  // 默认 32MB
    throw new Error('模型响应流过大，请缩小任务')
  
  let chunk: Record<string, unknown>
  try {
    chunk = record(JSON.parse(frame))
  } catch {
    throw new ModelFormatError('模型响应流格式错误，本轮未执行工具')
  }
  
  acceptUsage(chunk)  // 提取 Token 统计
  
  if (chunk.error)
    throw new Error(String(record(chunk.error).message || '模型服务返回错误'))
  
  const choice = record(Array.isArray(chunk.choices) ? chunk.choices[0] : null)
  const delta = record(choice.delta)
  
  let added = 0
  let phase: AgentModelProgress['phase'] = 'responding'
  
  // 处理思考内容
  if (typeof delta.reasoning_content === 'string' || 
      typeof delta.reasoning === 'string') {
    const text = String(delta.reasoning_content || delta.reasoning || '')
    options.onReasoning?.(text)
    reasoning += text
    added += text.length
    phase = 'thinking'
  }
  
  // 处理普通内容
  if (typeof delta.content === 'string') {
    options.onContent?.(delta.content)
    content += delta.content
    added += delta.content.length
    if (delta.content) phase = 'responding'
  }
  
  // 处理工具调用
  if (delta.tool_calls != null) {
    if (!Array.isArray(delta.tool_calls) || delta.tool_calls.length > 8)
      throw new ModelFormatError('模型工具调用格式错误或单轮超过 8 个')
    
    for (const raw of delta.tool_calls) {
      const call = record(raw)
      const fn = record(call.function)
      const index = call.index
      
      if (typeof index !== 'number' || !Number.isInteger(index) || 
          index < 0 || index > 7)
        throw new ModelFormatError('模型返回了无效的工具调用序号')
      
      const current = calls.get(index) || {
        id: '',
        type: 'function',
        function: { name: '', arguments: '' }
      }
      
      // 累加片段
      for (const [target, key, value] of [
        [current, 'id', call.id],
        [current.function, 'name', fn.name],
        [current.function, 'arguments', fn.arguments]
      ] as const) {
        if (value != null && typeof value !== 'string')
          throw new ModelFormatError('模型返回了无效的工具调用片段')
        if (typeof value === 'string') {
          (target as Record<string, string>)[key] += value
          added += value.length
        }
      }
      
      // 长度检查
      if (current.id.length > 200 ||
          current.function.name.length > 200 ||
          current.function.arguments.length > 150000)
        throw new Error('模型工具调用过长，请缩小任务')
      
      calls.set(index, current)
    }
    phase = 'tools'
  }
  
  // 防止结束后继续输出
  if (finish && added)
    throw new Error('模型在结束后返回了额外内容，本轮未执行工具')
  
  // 字符数限制
  characters += added
  if (characters > responseCharacterLimit)  // maxTokens * 8
    throw new Error('模型响应超过应用单轮容量，请缩小任务')
  
  // 进度回调
  if (added) {
    activity()  // 续期空闲超时
    options.onProgress?.({
      phase,
      characters,
      toolNames: phase === 'tools' 
        ? [...calls.values()].map(call => call.function.name).filter(Boolean)
        : undefined
    })
  }
  
  // 结束标记
  if (typeof choice.finish_reason === 'string' && choice.finish_reason) {
    finish = choice.finish_reason
  }
}
```

#### **llama.cpp 兼容性**
```typescript
// llama.cpp 的 JSON Schema 语法不支持某些关键字
const llamaGrammarOnlyKeywords = new Set([
  'minLength', 'maxLength', 'minimum', 'maximum',
  'exclusiveMinimum', 'exclusiveMaximum', 'multipleOf',
  'minItems', 'maxItems', 'minProperties', 'maxProperties', 'pattern'
])

function llamaSchema(value: unknown): unknown {
  if (Array.isArray(value)) return value.map(llamaSchema)
  if (!value || typeof value !== 'object') return value
  
  return Object.fromEntries(
    Object.entries(value as Record<string, unknown>)
      .filter(([key]) => !llamaGrammarOnlyKeywords.has(key))
      .map(([key, item]) => [key, llamaSchema(item)])
  )
}

// 检测本地 llama.cpp
if (connection.localLlama) {
  requestTools = llamaToolDefinitions(options.tools)
}
```

### 3. DeepSeek 特殊处理

```typescript
// 思考模式控制
function deepseekThinking(endpoint: string, thinking: boolean) {
  if (!isDeepSeek(endpoint)) return {}
  
  return thinking
    ? {}  // 默认开启，无需额外参数
    : { extra_body: { enable_thinking: false } }
}

// 消息格式
messages.map(({ reasoning_content, ...message }) => ({
  ...message,
  ...(isDeepSeek(connection.endpoint) && reasoning_content !== undefined
    ? { reasoning_content }
    : {}
  )
}))

// 响应处理
if (isDeepSeek(connection.endpoint)) {
  answer.reasoning_content = answer.reasoning || ''
}
```

### 4. Anthropic API 支持

```typescript
async function requestAnthropic(
  connection: AgentConnection,
  model: string,
  messages: AgentMessage[],
  signal: AbortSignal,
  options: RequestOptions,
  tools: ToolDefinition[] | false,
  responseCharacterLimit: number,
  responseWireLimit: number,
  acceptUsage: (data: unknown) => void,
  activity: () => void
): Promise<AgentAnswer> {
  // 提取系统消息
  const systemMessages = messages.filter(m => m.role === 'system')
  const system = systemMessages.map(m => 
    typeof m.content === 'string' ? m.content : ''
  ).join('\n\n')
  
  // 转换消息格式
  const anthropicMessages = messages
    .filter(m => m.role !== 'system')
    .map(m => ({
      role: m.role === 'tool' ? 'user' : m.role,
      content: anthropicMessageContent(m)
    }))
  
  // 构建请求
  const body = {
    model,
    messages: anthropicMessages,
    ...(system ? { system } : {}),
    ...(tools && tools.length ? {
      tools: tools.map(t => ({
        name: t.function.name,
        description: t.function.description,
        input_schema: t.function.parameters
      }))
    } : {}),
    max_tokens: connection.maxTokens,
    temperature: 0.2,
    stream: true
  }
  
  const response = await fetch(`${connection.endpoint}/messages`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'x-api-key': connection.key,
      'anthropic-version': '2023-06-01'
    },
    body: JSON.stringify(body),
    signal
  })
  
  // 流式解析 (SSE)
  // ...
}
```

### 5. Token 统计

```typescript
type TokenUsage = {
  promptTokens?: number
  completionTokens?: number
  totalTokens?: number
  reasoningTokens?: number  // DeepSeek 思考 Token
}

function readTokenUsage(data: unknown): TokenUsage | undefined {
  if (!data || typeof data !== 'object') return undefined
  const usage = (data as any).usage
  if (!usage || typeof usage !== 'object') return undefined
  
  return {
    promptTokens: typeof usage.prompt_tokens === 'number' 
      ? usage.prompt_tokens 
      : undefined,
    completionTokens: typeof usage.completion_tokens === 'number' 
      ? usage.completion_tokens 
      : undefined,
    totalTokens: typeof usage.total_tokens === 'number' 
      ? usage.total_tokens 
      : undefined,
    reasoningTokens: typeof usage.reasoning_tokens === 'number'
      ? usage.reasoning_tokens
      : undefined
  }
}

// llama.cpp timings 兼容
if (data.timings) {
  return {
    promptTokens: data.timings.prompt_n,
    completionTokens: data.timings.predicted_n,
    totalTokens: (data.timings.prompt_n || 0) + (data.timings.predicted_n || 0)
  }
}
```

### 6. 错误处理

```typescript
// 特定错误识别
if (response.status === 404 && code === 'model_not_found')
  throw new Error(`模型不存在或实例已失效（${model}）。请刷新模型列表，重新选择已加载的模型后继续任务。`)

if (response.status === 400 && 
    /failed to (?:initialize samplers: failed to )?parse grammar/i.test(text))
  throw new Error('本地模型服务无法解析工具调用语法。已使用 llama.cpp 兼容 Schema；若重启应用后仍出现此错误，请更新本地运行时并重新测试模型的工具能力。')

// 超时错误
if (record(error).name === 'TimeoutError' ||
    ['UND_ERR_HEADERS_TIMEOUT', 'UND_ERR_BODY_TIMEOUT']
      .includes(String(record(record(error).cause).code)))
  throw new Error('模型连接超时，本轮未执行工具。请检查模型服务或缩小上下文后继续任务。')

// 取消处理
if (signal.aborted) throw signal.reason
if (timeout.signal.aborted) throw timeout.signal.reason
```

---

## 📊 执行记录与恢复系统

### 1. 执行记录结构（execution-journal.ts）

#### **ExecutionRecord**
```typescript
type ExecutionRecord = {
  id: string                    // 事件 ID
  tool: string                  // 工具名称
  source: string                // 来源（built-in/python/mcp）
  revision?: string             // 工具版本
  argsFingerprint: string       // 参数指纹（SHA-256）
  stateFingerprint?: string     // 状态指纹（文件列表+哈希）
  expectedFiles?: FileExpectation[]  // 预期文件变化
  status: ExecutionStatus
  startedAt: string             // ISO 8601
  endedAt?: string
  durationMs?: number
  error?: string
  exitCode?: number | null
  termination?: 'cancelled' | 'timeout' | 'output-limit'
  userOutcome?: 'completed' | 'not-applied'  // 用户核对结果
  verifiedAt?: string
}

type ExecutionStatus = 
  | 'prepared'      // 已准备，未执行
  | 'running'       // 执行中
  | 'verifying'     // 验证中
  | 'succeeded'     // 成功
  | 'failed'        // 失败
  | 'unknown'       // 结果未知（需核对）
  | 'not-applied'   // 确认未执行

type FileExpectation = {
  path: string
  beforeHash: string | null  // 执行前 SHA-256
  afterHash: string          // 执行后 SHA-256
}
```

#### **存储位置**
```
{dataDir}/local-ai-agent-tasks/{taskId}/executions/{eventId}.json
```

#### **写入时机**
```typescript
// 1. 准备阶段（prepared）
await saveExecution({
  id: eventId,
  tool: name,
  source,
  revision,
  argsFingerprint: fingerprint(args),
  stateFingerprint: workspace.stateFingerprint(args, name),
  expectedFiles: prepared.expectedFiles,
  status: 'prepared',
  startedAt: new Date().toISOString()
})

// 2. 开始执行（running）
await updateExecution(eventId, { status: 'running' })

// 3. 执行完成（succeeded/failed）
await updateExecution(eventId, {
  status: 'succeeded',
  endedAt: new Date().toISOString(),
  durationMs: Date.now() - started
})

// 4. 用户核对（不修改 status，记录 userOutcome）
await updateExecution(eventId, {
  userOutcome: 'completed',
  verifiedAt: new Date().toISOString()
})
```

### 2. 中断恢复流程

#### **恢复策略**
```typescript
async function reconcileExecution(
  workspace: AgentWorkspace,
  record: ExecutionRecord
): Promise<'verified' | 'failed' | 'unknown'> {
  
  if (!record.expectedFiles || record.expectedFiles.length === 0) {
    // 命令或外部操作，无法自动验证
    return 'unknown'
  }
  
  let allMatch = true
  let anyMatch = false
  
  for (const expected of record.expectedFiles) {
    try {
      const file = workspace.resolve(expected.path, true)
      const current = fs.existsSync(file)
        ? digest(fs.readFileSync(file))
        : null
      
      if (current === expected.afterHash) {
        // 匹配目标哈希：已完成
        anyMatch = true
      } else if (current === expected.beforeHash) {
        // 匹配基线哈希：未执行
        allMatch = false
      } else {
        // 不匹配：部分执行或被修改
        return 'unknown'
      }
    } catch {
      return 'unknown'
    }
  }
  
  if (allMatch && anyMatch) {
    // 所有文件都匹配目标
    return 'verified'
  } else if (!anyMatch) {
    // 所有文件都匹配基线
    return 'failed'
  } else {
    // 部分匹配
    return 'unknown'
  }
}
```

#### **任务恢复检查**
```typescript
async function checkRecovery(task: AgentTask): Promise<RecoveryStatus> {
  const executions = await loadExecutions(task.id)
  
  const unknown = executions.filter(e => 
    e.status === 'running' || 
    e.status === 'verifying' ||
    (e.status === 'succeeded' && !e.userOutcome)
  )
  
  if (unknown.length === 0) {
    return { canResume: true, needsReconciliation: false }
  }
  
  // 尝试自动核对
  const reconciled = await Promise.all(
    unknown.map(e => reconcileExecution(workspace, e))
  )
  
  const stillUnknown = reconciled.filter(r => r === 'unknown').length
  
  if (stillUnknown > 0) {
    return {
      canResume: false,
      needsReconciliation: true,
      unknownCount: stillUnknown,
      message: `有 ${stillUnknown} 个步骤结果未知，需要用户核对`
    }
  }
  
  // 自动更新状态
  for (let i = 0; i < unknown.length; i++) {
    const result = reconciled[i]
    if (result === 'verified') {
      await updateExecution(unknown[i].id, {
        status: 'succeeded',
        userOutcome: 'completed',
        verifiedAt: new Date().toISOString()
      })
    } else if (result === 'failed') {
      await updateExecution(unknown[i].id, {
        status: 'not-applied',
        userOutcome: 'not-applied',
        verifiedAt: new Date().toISOString()
      })
    }
  }
  
  return { canResume: true, needsReconciliation: false }
}
```

### 3. 只读模式

```typescript
// unknown 步骤时的限制
if (hasUnknownExecutions(task)) {
  // 允许只读工具
  if (readTools.has(toolName)) {
    return executeReadTool(toolName, args)
  }
  
  // 允许 reconcile_execution
  if (toolName === 'reconcile_execution') {
    return reconcileAllExecutions(task)
  }
  
  // 阻止写入和命令
  throw new Error(
    '任务包含未核对的步骤，请先使用 reconcile_execution 核对文件，' +
    '或在界面中确认"已核对完成"/"确认未执行"后继续。' +
    '核对不会重新执行旧操作。'
  )
}
```

---

## 📈 性能特性

### 1. 执行效率

- **Python 并发**: 最多 8 个独立进程
- **命令输出**: 默认 2MB 上限，可配置
- **进程树终止**: 
  - Windows: 递归终止子进程
  - Unix: 进程组 SIGKILL
- **UTF-8 保证**: `StringDecoder` 正确处理多字节字符
- **流式输出**: 实时回调，不等待完成

### 2. 内存管理

- **大结果外存**: 超过预算保存到 `results/{taskId}/{eventId}.txt`
- **分页读取**: `read_tool_result` 按 offset 读取
- **上下文压缩**: 自动生成摘要，保留最新要求
- **原始历史**: 不删除，支持 `read_history` 查询
- **结果 ID 索引**: 固定进入系统提示

### 3. 响应优化

- **流式解析**: SSE 逐帧处理
- **进度回调**: 实时更新 phase 和 characters
- **增量渲染**: 界面异步更新
- **活动检测**: 空闲超时自动续期
- **缓存策略**: 沙箱能力缓存 30 秒

### 4. 资源限制

- **单次响应**: 
  - 字符: `maxTokens * 8`
  - 原始流: `maxTokens * 8 * 3` (约 32MB)
- **工具 Schema**: 
  - 单工具: 最多 150KB 参数
  - 总数: 最多 100 个工具
- **MCP 响应**: 单次最多 1MB
- **Python 输出**: stdout 1MB, stderr 20KB
- **文件读取**: 单文件最多 2MB

---

## 🎯 核心优势与创新点

### 1. **本地优先 + 混合部署**
- 完全离线运行（llama.cpp + GGUF）
- 远程 API 无缝切换（DeepSeek/OpenAI 兼容）
- 统一接口透明调用
- 数据本地存储，隐私可控

### 2. **可靠的执行记录系统**
- **独立执行日志**: `executions/{taskId}/{eventId}.json`
- **文件哈希验证**: SHA-256 before/after 对比
- **中断恢复机制**: 自动核对 + 用户确认
- **状态机设计**: 7 种状态精确追踪
- **只读模式**: 未核对步骤时仅允许查询

### 3. **三层工具扩展架构**
- **内置工具**: 50+ 专业工具，按需加载
- **Python 工具**: 进程级隔离，最多 8 并发
- **MCP 协议**: 标准化第三方集成
- **动态选择**: Token 预算 + 优先级管理

### 4. **多维度安全保障**
- **容器沙箱**: Docker/Podman 隔离（网络断开、资源限制）
- **文件权限**: 路径穿越/符号链接/密钥检测
- **版本校验**: 写入前后双重验证
- **原子操作**: 临时文件 + 重命名
- **自动回退**: 多文件补丁失败恢复

### 5. **优秀的上下文管理**
- **自动压缩**: 保留最新要求 + 工具调用
- **大结果分页**: 外存 + `read_tool_result`
- **小模型优化**: ≤8K 容量特殊处理
- **恢复策略**: 从摘要 + 执行记录重建

### 6. **完善的流式处理**
- **SSE 逐帧解析**: 实时进度反馈
- **多阶段追踪**: thinking / responding / tools
- **UTF-8 保证**: `StringDecoder` 多字节处理
- **超时控制**: 三级超时（首次/空闲/总计）
- **增量限制**: 字符数 + 原始流双重检查

---

## 📐 架构设计评价

### 优秀设计

1. **职责分离清晰**
   - `execution-sandbox.ts`: 沙箱能力检测与请求构建
   - `execution-supervisor.ts`: 进程监控与 UTF-8 解码
   - `execution-journal.ts`: 执行记录管理
   - `workspace.ts`: 文件权限与工作区隔离
   - `model.ts`: 模型交互与流式解析

2. **可扩展性强**
   - 工具注册表 (`ToolRegistry`) 统一管理
   - 专业包 (`load_tool_pack`) 按需加载
   - MCP 协议标准化第三方集成
   - Python 独立进程隔离

3. **容错性好**
   - 执行记录系统支持中断恢复
   - 多文件补丁自动回退
   - 文件版本校验防止冲突
   - 流式解析严格错误检查

4. **性能优化**
   - Token 预算动态分配
   - 大结果外存分页
   - 沙箱能力缓存
   - Python 进程池管理

5. **安全第一**
   - 多层权限控制（read-only/low/high）
   - 容器沙箱隔离
   - 凭据加密存储
   - 审计日志完整

### 潜在改进空间

1. **进程沙箱**
   - 当前非操作系统级沙箱
   - 依赖 Docker/Podman 外部工具
   - 可考虑：Landlock (Linux)、Seatbelt (macOS)、Job Objects (Windows)

2. **工具版本管理**
   - Python 脚本修改后自动降级为 high 风险
   - 可考虑：工具签名、版本锁定、差异对比

3. **恢复完整性**
   - 命令执行无法验证副作用
   - 进程强制终止时回退不保证
   - 可考虑：事务日志、Checkpoint 机制

4. **MCP 稳定性**
   - 不自动重连
   - 不重放调用
   - 可考虑：连接池、重试策略、幂等性标记

5. **测试覆盖**
   - 143 项自动化测试（优秀）
   - 可增加：压力测试、并发测试、边界条件

---

## 🔍 代码质量评估

### 代码风格
- ✅ TypeScript 严格模式
- ✅ 类型定义完整（shared/）
- ✅ 函数职责单一
- ✅ 错误处理统一（ToolError）
- ✅ 注释适度（关键逻辑有说明）

### 命名规范
- ✅ 驼峰命名一致
- ✅ 类型后缀清晰（Record/Status/Result）
- ✅ 常量大写（maxFile/ignored）
- ✅ 私有方法 private 标记

### 模块化
- ✅ 按功能拆分文件（~3768 行 / 15 文件 = 平均 251 行/文件）
- ✅ 共享类型独立（shared/）
- ✅ 工具类独立（registry.ts/tool-store.ts）
- ✅ 最小依赖原则

### 可维护性
- ✅ 状态机明确（ExecutionStatus）
- ✅ 配置外部化（环境变量）
- ✅ 日志记录完善（ApplicationLogger）
- ✅ 测试覆盖充分

---

## 📚 适用场景分析

### ✅ 非常适合

1. **本地代码开发**
   - 代码分析、重构、测试
   - 构建脚本调试
   - Git 操作自动化

2. **文档批处理**
   - DOCX/PDF/XLSX 批量转换
   - 文档内容提取与整理
   - 模板生成

3. **隐私敏感任务**
   - 企业内部代码审查
   - 敏感数据处理
   - 合规要求的离线环境

4. **混合 AI 工作流**
   - 弱模型本地运行（快速迭代）
   - 强模型远程调用（复杂任务）
   - 定时任务自动化

5. **工具链集成**
   - MCP 协议接入第三方服务
   - Python 自定义工具
   - 工作流编排

### ⚠️ 需谨慎

1. **高并发场景**
   - Python 最多 8 并发
   - 无任务队列调度
   - 单机资源限制

2. **大规模代码库**
   - 文件遍历最多 5000 个
   - 检索结果最多 60 条
   - 可能需要索引优化

3. **实时交互**
   - 命令执行有超时限制
   - 流式响应有网络依赖
   - 首次响应最多等待 10 分钟

4. **生产环境部署**
   - 非系统级沙箱
   - 需人工确认操作
   - 不适合完全无人值守

---

## 🚀 未来发展建议

### 短期优化（P1）

1. **工具索引优化**
   - 语义搜索（Embedding）
   - 全文索引（Tantivy/MeiliSearch）
   - 缓存热点工具 Schema

2. **并发增强**
   - Python 工作池动态扩展
   - MCP 连接池复用
   - 任务队列管理

3. **测试增强**
   - 边界条件覆盖
   - 并发压力测试
   - 沙箱回归（多平台）

4. **文档完善**
   - API 文档生成（TypeDoc）
   - 工具使用示例
   - 故障排查指南

### 中期扩展（P2）

1. **系统级沙箱**
   - Linux: Landlock/seccomp-bpf
   - macOS: Seatbelt
   - Windows: Job Objects/AppContainer

2. **分布式执行**
   - 任务分发器
   - 远程工作节点
   - 结果聚合

3. **可观测性**
   - Metrics 导出（Prometheus）
   - Tracing 集成（OpenTelemetry）
   - 实时监控面板

4. **插件市场**
   - 工具发布平台
   - 版本管理
   - 签名验证

### 长期演进（P3）

1. **多 Agent 协作**
   - Agent 间通信协议
   - 任务委托与编排
   - 共享上下文管理

2. **持续学习**
   - 用户反馈收集
   - 工具使用统计
   - 自动调优建议

3. **企业增强**
   - RBAC 权限体系
   - 审计合规报告
   - SSO 集成
   - 租户隔离

4. **云原生**
   - K8s Operator
   - 弹性扩缩容
   - 多租户 SaaS

---

## 📊 技术栈总结

### 核心技术
| 层次 | 技术 | 用途 |
|------|------|------|
| 前端 | Vue 3.5 + Element Plus | UI 渲染与交互 |
| 主进程 | Electron 41 + Node 22 | 桌面应用与系统调用 |
| 类型系统 | TypeScript 5.9 | 静态类型检查 |
| 构建工具 | Vite 6.4 | 前端构建与热更新 |
| 打包工具 | electron-builder 26 | 跨平台打包 |

### 模型交互
| 组件 | 用途 |
|------|------|
| llama.cpp | 本地 GGUF 模型推理 |
| OpenAI API | 远程模型调用（兼容） |
| Anthropic API | Claude 模型支持 |
| DeepSeek API | 中文优化模型 |

### 工具生态
| 类型 | 实现 |
|------|------|
| 内置工具 | TypeScript 实现（50+ 工具） |
| Python 工具 | 独立进程 + JSON-RPC |
| MCP 工具 | @modelcontextprotocol/client |
| 文档处理 | exceljs, pdfjs-dist, jszip |

### 安全组件
| 组件 | 用途 |
|------|------|
| Docker/Podman | 容器沙箱隔离 |
| safeStorage | 凭据加密存储 |
| crypto | 文件哈希与指纹 |
| 路径解析 | 穿越检测与符号链接拒绝 |

---

## 📖 文档资源

### 官方文档
- [Agent 实施方案](docs/local-ai-agent.md) - 3768 行核心代码详解
- [模型与运行时](docs/local-ai-studio.md) - 模型管理与推理
- [规划与边界](docs/local-ai-roadmap.md) - 产品路线图
- [拆分验证](docs/extraction-validation.md) - 从 MyPlane 拆分记录

### 关键源码
```
electron/main/agent/
├── execution-sandbox.ts      # 容器沙箱（277 行）
├── execution-supervisor.ts   # 进程监控（54 行）
├── execution-journal.ts      # 执行记录
├── workspace.ts              # 工作区管理（150 行）
├── tools.ts                  # 工具定义（53 行）
├── python.ts                 # Python 运行时（90 行）
├── mcp.ts                    # MCP 管理（77 行）
├── model.ts                  # 模型交互（120+ 行）
└── ...
```

### 测试用例
```
tests/
├── local-ai-execution.test.mjs          # 执行记录测试
├── local-ai-small-context.test.mjs      # 小上下文测试
├── local-ai-execution.electron.mjs      # Electron 回归
└── ...                                   # 共 143 项
```

---

## 🎓 总结与评价

### 整体评价：⭐⭐⭐⭐⭐ (5/5)

MyPlaneAgent 是一个**工程质量极高**的本地 AI Agent 实现，展现了以下特点：

#### 架构设计 ⭐⭐⭐⭐⭐
- 三层架构清晰（前端/主进程/工具层）
- 职责分离合理（沙箱/监控/记录/工作区）
- 扩展性优秀（工具注册表 + MCP 协议）
- 容错性强（执行记录 + 中断恢复）

#### 代码质量 ⭐⭐⭐⭐⭐
- TypeScript 严格模式 + 完整类型定义
- 平均 251 行/文件，模块化合理
- 错误处理统一（ToolError）
- 143 项自动化测试覆盖

#### 安全性 ⭐⭐⭐⭐½
- 容器沙箱隔离（网络/文件系统/资源）
- 文件权限多重检查（穿越/符号链接/密钥）
- 凭据加密存储 + MCP 跨域保护
- 审计日志完整
- *减半分：非系统级沙箱，依赖外部容器*

#### 可靠性 ⭐⭐⭐⭐⭐
- 执行记录系统（7 种状态追踪）
- 文件哈希验证（before/after）
- 中断恢复机制（自动核对 + 用户确认）
- 多文件补丁自动回退
- UTF-8 保证 + 流式健壮性

#### 性能 ⭐⭐⭐⭐
- Token 预算动态分配
- 大结果外存分页
- Python 最多 8 并发
- 沙箱能力缓存
- *可优化：并发限制、索引性能*

#### 易用性 ⭐⭐⭐⭐⭐
- 50+ 内置工具开箱即用
- MCP 协议标准化集成
- 工作流可视化编排
- 定时任务自动化
- 详尽文档 + 测试示例

### 适合学习的设计模式

1. **执行记录系统**: 如何设计可靠的 Agent 中断恢复
2. **三层工具架构**: 内置 + Python + MCP 的扩展模型
3. **流式解析**: SSE 多阶段进度追踪实现
4. **沙箱设计**: 容器隔离的实践案例
5. **Token 管理**: 动态预算分配策略

### 推荐指数

| 用户类型 | 推荐度 | 理由 |
|---------|--------|------|
| Agent 开发者 | ⭐⭐⭐⭐⭐ | 优秀的架构参考与实践案例 |
| 企业用户 | ⭐⭐⭐⭐⭐ | 本地部署 + 隐私保护 + 混合 AI |
| 个人开发者 | ⭐⭐⭐⭐ | 功能强大，但需要理解复杂度 |
| 学习者 | ⭐⭐⭐⭐⭐ | 代码质量高，文档完善 |
| 生产环境 | ⭐⭐⭐⭐ | 需人工确认，非完全自动化 |

---

**报告生成时间**: 2026-09-24  
**分析版本**: v0.1.0  
**分析依据**: 源代码完整审查（3768 行核心 + 测试用例）  
**报告作者**: Claude (Opus 4.6) via Kiro
