# MyPlaneAgent 界面适配新框架 - 实施指南

## 执行摘要

本文档记录了将 MyPlaneAgent 前端界面适配到新的 Agent Core 架构的完整方案。新架构基于模块化的 5 层设计（Agent Core → Capability Registry → Skill Platform/MCP Adapter），提供更强大的任务规划、执行和恢复能力。

## 一、架构现状分析

### 1.1 当前状态

**新架构已实现但未集成：**
- ✅ Agent Core 架构完整实现（`electron/main/agent/core/`）
- ✅ 组件包括：AgentCore, CapabilityRegistry, SkillPlatform, AgentPlanner, AgentExecutor, AgentMemory
- ❌ **仅在测试和示例代码中使用**
- ❌ **生产界面仍使用旧的 `agent/service.ts`**

**核心问题：**
- 前端界面（LocalAiAgent.vue）与旧架构深度耦合
- 缺少新架构的服务层封装
- 没有新功能的界面展示（规划步骤、能力注册表等）

### 1.2 适配目标

1. **保持向后兼容**：旧功能继续可用
2. **渐进式迁移**：新旧系统并存，逐步切换
3. **增强用户体验**：展示任务规划、执行进度、能力管理
4. **提升可维护性**：清晰的服务边界，模块化组件

---

## 二、已完成的工作

### 2.1 后端服务层

#### ✅ AgentCoreService（`electron/main/agent/agent-core-service.ts`）

**功能：**
- 封装 Agent Core 架构的初始化和管理
- 提供与旧 LocalAgentService 兼容的接口
- 事件转发机制（planning_started, plan_created, execution_started 等）
- 任务运行、取消、状态查询

**核心方法：**
```typescript
- initialize(): Promise<void>
- runTask(task, options): Promise<void>
- cancelTask(taskId): void
- listCapabilities()
- listSkills()
- addMCPServer(config)
- removeMCPServer(id)
```

#### ✅ 集成补丁（`electron/main/local-ai-studio-integration.patch.ts`）

**内容：**
- 如何在 LocalAiStudioService 中初始化 AgentCoreService
- IPC 处理程序注册
- 事件转发到前端
- 资源清理

**关键特性：**
- 功能开关（`agentCoreEnabled`）控制新旧架构切换
- 初始化失败自动回退到旧系统
- 异步初始化不阻塞应用启动

### 2.2 前端组件

#### ✅ AgentCapabilityRegistry.vue（`src/local-ai/AgentCapabilityRegistry.vue`）

**功能：**
- 显示所有可用能力（内置、Python Skills、MCP 工具）
- 按分类和来源过滤
- 搜索能力
- 启用/禁用能力
- 查看能力参数和详情

**界面特性：**
- 分组显示（文件操作、Git、文档、网络等）
- 来源标记（内置、Skill、MCP）
- 状态指示（active, inactive, error）
- 可展开的参数表格
- 响应式设计

---

## 三、待实施工作

### 3.1 后端集成（高优先级）

#### 任务 #3: 更新 local-ai-studio.ts

**工作内容：**

1. **导入新模块**
```typescript
import { AgentCoreService } from './agent/agent-core-service.js'
```

2. **添加类属性**
```typescript
private agentCoreService: AgentCoreService | null = null
private agentCoreEnabled = false
```

3. **构造函数中初始化**
```typescript
this.agentCoreService = new AgentCoreService({
  dataDir: path.join(dataRoot, 'agent-core'),
  skillsDir: path.join(dataRoot, 'skills'),
  getConnection: () => this.service(),
  diagnosticLog: message => log?.("error", "agent-core", message)
})
```

4. **注册 IPC 处理程序**
```typescript
ipcMain.handle('agent-core-status', () => service.agentCoreStatus())
ipcMain.handle('agent-core-run-task', (e, taskId, desc, opts) => 
  service.agentCoreRunTask(taskId, desc, opts))
// ... 其他处理程序
```

5. **在 dispose 中清理资源**
```typescript
if (this.agentCoreService) {
  await this.agentCoreService.dispose()
}
```

**估计工时：** 2-4 小时

---

### 3.2 前端界面适配（高优先级）

#### 任务 #4: 更新 LocalAiAgent.vue

**工作内容：**

1. **添加架构模式切换**
```typescript
const useNewArchitecture = ref(false)
const agentCoreStatus = ref({ enabled: false, available: false })

// 检查新架构可用性
async function checkAgentCore() {
  const status = await window.myplane.localAiStudio('agentCoreStatus')
  agentCoreStatus.value = status
  if (status.enabled) {
    useNewArchitecture.value = true
  }
}
```

2. **监听新架构事件**
```typescript
window.myplane.onAgentCoreEvent((event) => {
  switch (event.event.type) {
    case 'planning_started':
      showPlanningIndicator()
      break
    case 'plan_created':
      displayPlan(event.event.plan)
      break
    case 'execution_started':
      showExecutionProgress()
      break
    // ... 其他事件
  }
})
```

3. **显示任务规划**
```vue
<div v-if="currentPlan" class="task-plan">
  <h3>执行计划</h3>
  <ol class="plan-steps">
    <li v-for="step in currentPlan.steps" :key="step.id">
      <strong>{{ step.capability }}</strong>
      <p>{{ step.description }}</p>
    </li>
  </ol>
  <p class="plan-reasoning">{{ currentPlan.reasoning }}</p>
</div>
```

4. **添加架构切换开关**
```vue
<div class="architecture-toggle">
  <label>
    <input type="checkbox" v-model="useNewArchitecture" 
           :disabled="!agentCoreStatus.available" />
    使用新架构（支持智能规划和失败恢复）
  </label>
</div>
```

**估计工时：** 4-6 小时

---

#### 任务 #5: 适配 Skill Manager

**工作内容：**

1. **连接到 SkillPlatform**
```typescript
const skills = ref([])

async function loadSkills() {
  const result = await window.myplane.localAiStudio('agentCoreListSkills')
  skills.value = result
}
```

2. **显示 Skill 详细信息**
```vue
<div class="skill-card">
  <h3>{{ skill.name }}</h3>
  <p>{{ skill.description }}</p>
  <div class="skill-tools">
    <h4>工具列表</h4>
    <ul>
      <li v-for="tool in skill.tools" :key="tool.name">
        {{ tool.name }} - {{ tool.description }}
      </li>
    </ul>
  </div>
</div>
```

3. **添加 Skill 状态指示**
```vue
<span class="skill-status" :class="`status-${skill.status}`">
  {{ getStatusText(skill.status) }}
</span>
```

**估计工时：** 3-4 小时

---

#### 任务 #7: 增强任务执行可视化

**工作内容：**

1. **创建执行进度组件**
```vue
<template>
  <div class="execution-progress">
    <div class="progress-header">
      <h3>执行进度</h3>
      <span class="status-badge" :class="executionStatus">
        {{ statusText }}
      </span>
    </div>

    <!-- 进度条 -->
    <div class="progress-bar">
      <div class="progress-fill" :style="{ width: `${progress}%` }"></div>
    </div>

    <!-- 当前步骤 -->
    <div class="current-step">
      <strong>当前步骤：</strong>
      <span>{{ currentStep }}</span>
    </div>

    <!-- 步骤列表 -->
    <div class="steps-list">
      <div
        v-for="(step, index) in steps"
        :key="index"
        class="step-item"
        :class="getStepClass(step)"
      >
        <div class="step-icon">
          <Check v-if="step.status === 'completed'" />
          <Loading v-else-if="step.status === 'running'" />
          <Clock v-else />
        </div>
        <div class="step-content">
          <strong>{{ step.capability }}</strong>
          <p>{{ step.description }}</p>
          <span v-if="step.error" class="step-error">{{ step.error }}</span>
        </div>
        <div class="step-duration">{{ formatDuration(step.duration) }}</div>
      </div>
    </div>

    <!-- 重试信息 -->
    <div v-if="retryCount > 0" class="retry-info">
      <Warning />
      <span>已重试 {{ retryCount }} 次</span>
    </div>
  </div>
</template>
```

2. **实时更新执行状态**
```typescript
window.myplane.onAgentCoreEvent((event) => {
  if (event.event.type === 'execution_started') {
    executionStatus.value = 'running'
  } else if (event.event.type === 'execution_completed') {
    executionStatus.value = 'completed'
    progress.value = 100
  } else if (event.event.type === 'replanning_started') {
    retryCount.value++
    replanningReason.value = event.event.reason
  }
})
```

**估计工时：** 4-5 小时

---

### 3.3 UI/UX 增强（中优先级）

#### 界面集成点

1. **主导航添加"能力注册表"入口**
```typescript
const navigation = [
  { id: 'agent', label: '工作台', icon: ChatDotRound },
  { id: 'workflow', label: '工作流', icon: Share },
  { id: 'capabilities', label: '能力注册表', icon: Grid }, // 新增
  { id: 'skills', label: 'Skills', icon: Operation },
  // ...
]
```

2. **LocalAiStudio.vue 中添加路由**
```vue
<AgentCapabilityRegistry v-if="tab === 'capabilities'" />
```

3. **添加架构状态指示器**
```vue
<div class="architecture-status">
  <span v-if="agentCoreEnabled" class="status-enabled">
    <Check /> 新架构已启用
  </span>
  <span v-else class="status-legacy">
    <InfoFilled /> 使用传统模式
  </span>
</div>
```

**估计工时：** 2-3 小时

---

### 3.4 类型定义和共享代码（必需）

#### 添加类型定义到 `electron/shared/types/index.ts`

```typescript
export interface AgentTask {
  id: string
  description: string
  context: {
    workspace: string
  }
  createdAt: number
}

export interface AgentPlan {
  steps: AgentPlanStep[]
  reasoning: string
}

export interface AgentPlanStep {
  id: string
  capability: string
  args: Record<string, unknown>
  description: string
}

export interface ExecutionResult {
  success: boolean
  outputs: unknown[]
  errors: string[]
  elapsedMs: number
}

export type AgentEventType =
  | 'task_created'
  | 'planning_started'
  | 'plan_created'
  | 'execution_started'
  | 'execution_completed'
  | 'task_failed'
  | 'replanning_started'

export interface AgentEvent {
  type: AgentEventType
  taskId?: string
  plan?: AgentPlan
  result?: ExecutionResult
  error?: string
  reason?: string
}

export interface CapabilityInfo {
  name: string
  category: string
  source: 'builtin' | 'skill' | 'mcp'
  description: string
  parameters: Array<{
    name: string
    type: string
    required: boolean
    description?: string
  }>
}
```

**估计工时：** 1-2 小时

---

## 四、实施计划

### 阶段 1: 后端集成（第1周）

**任务：**
1. ✅ 创建 AgentCoreService（已完成）
2. ✅ 创建集成补丁文档（已完成）
3. ⏳ 修改 local-ai-studio.ts
4. ⏳ 添加类型定义
5. ⏳ 测试后端集成

**验收标准：**
- AgentCoreService 成功初始化
- IPC 调用正常工作
- 事件正确转发到前端
- 旧功能不受影响

---

### 阶段 2: 前端适配（第2周）

**任务：**
1. ⏳ 更新 LocalAiAgent.vue
2. ✅ 创建 AgentCapabilityRegistry.vue（已完成）
3. ⏳ 适配 AgentSkillManager.vue
4. ⏳ 创建执行进度组件
5. ⏳ 添加导航入口

**验收标准：**
- 可以切换新旧架构
- 显示任务规划步骤
- 能力注册表可查看和管理
- 执行进度实时更新

---

### 阶段 3: 测试和优化（第3周）

**任务：**
1. ⏳ 端到端测试
2. ⏳ 性能优化
3. ⏳ 错误处理完善
4. ⏳ 用户文档

**验收标准：**
- 所有功能正常工作
- 无明显性能问题
- 错误提示清晰
- 文档完整

---

## 五、关键技术决策

### 5.1 渐进式迁移策略

**选择：** 新旧系统并存，通过功能开关切换

**理由：**
- 降低风险，出问题可快速回退
- 用户可以选择使用哪个模式
- 逐步验证新架构的稳定性

**实现：**
```typescript
if (agentCoreEnabled && useNewArchitecture) {
  // 使用新架构
  await agentCoreService.runTask(task, options)
} else {
  // 使用旧架构
  await agent.run(task)
}
```

### 5.2 事件驱动的界面更新

**选择：** 通过 IPC 事件流更新界面状态

**理由：**
- 实时反馈任务进度
- 解耦后端和前端
- 支持多窗口同步

**实现：**
```typescript
// 后端
agentCore.on((event) => {
  webContents.send('agent-core-event', { taskId, event })
})

// 前端
window.myplane.onAgentCoreEvent((data) => {
  updateUIState(data)
})
```

### 5.3 能力注册表作为统一入口

**选择：** 所有能力（内置、Skill、MCP）统一在注册表管理

**理由：**
- 统一的发现和调用接口
- 方便启用/禁用能力
- 清晰的权限和来源标识

**实现：**
- CapabilityRegistry 聚合所有来源
- 前端通过单一 API 获取能力列表
- 用户可视化管理所有能力

---

## 六、测试策略

### 6.1 单元测试

**覆盖范围：**
- AgentCoreService 初始化
- 任务运行和取消
- 事件转发机制
- 能力列表查询

**工具：** 现有的测试框架（test-e2e-real.ts）

### 6.2 集成测试

**测试场景：**
1. 新架构运行简单任务
2. 任务失败后自动重新规划
3. 切换新旧架构
4. 同时运行多个任务
5. 取消运行中的任务

### 6.3 UI 测试

**测试内容：**
1. 能力注册表显示正确
2. 任务规划步骤展示
3. 执行进度实时更新
4. 错误信息显示
5. 架构切换开关工作

### 6.4 兼容性测试

**验证点：**
- 旧功能继续可用
- 旧任务可以正常查看
- 数据迁移无损
- 性能无明显下降

---

## 七、风险和应对

### 7.1 潜在风险

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 新架构性能不及预期 | 高 | 中 | 性能测试，优化关键路径 |
| 兼容性问题 | 中 | 中 | 充分测试，保留回退机制 |
| 用户学习成本 | 低 | 高 | 提供清晰文档和 UI 提示 |
| 数据迁移问题 | 高 | 低 | 保持数据格式兼容 |

### 7.2 回退计划

**如果新架构出现严重问题：**

1. **即时回退**
```typescript
// 全局禁用新架构
agentCoreEnabled = false
```

2. **代码回退**
- 新代码使用功能开关保护
- 可以通过配置文件禁用
- 旧代码完全保留

3. **数据回滚**
- 新旧数据分开存储
- 不修改旧数据格式
- 可以独立恢复

---

## 八、后续优化方向

### 8.1 短期（1-2个月）

- [ ] 完善 Skill 管理界面
- [ ] 添加任务历史和统计
- [ ] 优化大量能力时的性能
- [ ] 改进错误提示和日志

### 8.2 中期（3-6个月）

- [ ] 可视化工作流编辑器
- [ ] 任务模板和复用
- [ ] 能力市场和分享
- [ ] 高级权限管理

### 8.3 长期（6-12个月）

- [ ] 完全移除旧架构
- [ ] 多 Agent 协作
- [ ] 云端同步和共享
- [ ] 插件生态系统

---

## 九、总结

### 已完成

✅ AgentCoreService 服务层  
✅ 集成补丁文档  
✅ AgentCapabilityRegistry 组件  
✅ 详细的实施计划

### 剩余工作

⏳ 后端集成到 local-ai-studio.ts  
⏳ 前端界面适配  
⏳ 类型定义  
⏳ 测试和文档

### 总工时估算

- 后端集成: 3-6 小时
- 前端适配: 13-18 小时
- 测试优化: 8-12 小时
- **总计: 24-36 小时（3-5 个工作日）**

### 关键成功因素

1. **渐进式迁移**：新旧并存，降低风险
2. **清晰的服务边界**：AgentCoreService 封装复杂性
3. **良好的用户体验**：可视化规划和进度
4. **完善的测试**：确保稳定性和兼容性

---

## 附录

### A. 相关文件清单

**已创建：**
- `electron/main/agent/agent-core-service.ts` - 核心服务层
- `electron/main/local-ai-studio-integration.patch.ts` - 集成补丁
- `src/local-ai/AgentCapabilityRegistry.vue` - 能力注册表界面

**需要修改：**
- `electron/main/local-ai-studio.ts` - 添加 AgentCoreService
- `src/local-ai/LocalAiAgent.vue` - 适配新架构事件
- `src/local-ai/AgentSkillManager.vue` - 连接 SkillPlatform
- `src/local-ai/WorkspaceChatMessages.vue` - 显示执行进度
- `src/LocalAiStudio.vue` - 添加导航入口
- `electron/shared/types/index.ts` - 添加类型定义

### B. 参考文档

- [README.md](../README.md) - 项目概述
- [docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) - 架构文档
- [docs/MIGRATION.md](../docs/MIGRATION.md) - 迁移指南
- [docs/UI_INTEGRATION.md](../docs/UI_INTEGRATION.md) - UI 集成指南

---

**文档版本**: 1.0  
**创建日期**: 2026-09-24  
**最后更新**: 2026-09-24  
**作者**: Claude (Opus 5)
