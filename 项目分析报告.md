# MyPlaneAgent 项目分析报告

## 1. 项目概述

**项目名称**: MyPlaneAgent  
**版本**: 0.1.0  
**定位**: 独立的本地 AI Agent 桌面工作空间  
**技术栈**: Electron + Vue 3 + TypeScript  
**最后更新**: 2024年9月

### 核心特性
- 🧠 **智能规划**: LLM 驱动的任务分解和执行计划生成
- 🔄 **自动恢复**: 失败后自动重新规划，最多重试 3 次
- 🧩 **模块化架构**: 5 层清晰分层，易于扩展和维护
- 🐍 **Python Native Skills**: 使用 Python 编写自定义技能
- 🔌 **MCP 集成**: 支持标准 MCP 协议，集成外部服务
- 📝 **记忆系统**: 学习成功案例，提供历史参考

---

## 2. 技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────┐
│         Agent Core (核心层)              │
│  - 任务规划 (Planner)                    │
│  - 执行引擎 (Executor)                   │
│  - 记忆管理 (Memory)                     │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│    Capability Registry (能力注册表)      │
│  - 统一接口                              │
│  - 能力路由                              │
└─────────────────────────────────────────┘
                    ↓
        ┌───────────┴───────────┐
        ↓                       ↓
┌──────────────────┐  ┌──────────────────┐
│  Skill Platform  │  │   MCP Adapter    │
│  - Skill 加载    │  │  - MCP 服务管理  │
└──────────────────┘  └──────────────────┘
        ↓                       ↓
┌──────────────────┐  ┌──────────────────┐
│ Python Runtime   │  │   MCP Servers    │
│  - 进程池        │  │  - 外部服务      │
└──────────────────┘  └──────────────────┘
```

### 2.2 核心组件

#### Agent Core (electron/main/agent/core/)
- **agent-core.ts**: 主流程控制，整合规划、执行、记忆
- **agent-planner.ts**: LLM 驱动的任务分解和计划生成
- **agent-executor.ts**: 执行引擎，按计划调用能力
- **agent-memory.ts**: 记忆管理，学习成功案例
- **capability-registry.ts**: 能力注册表，统一工具接口
- **model-client.ts**: 模型调用客户端，支持多种 AI 提供商

#### Skill Platform
- **skill-platform.ts**: Skill 加载和生命周期管理
- **python-runtime-manager.ts**: Python Worker 池管理
- JSON-RPC 协议通信
- 进程隔离执行

#### MCP Integration
- **mcp-adapter.ts**: MCP 协议适配器
- **mcp-client.ts**: MCP 客户端实现
- 支持 stdio 和 HTTP 传输
- 统一工具发现和调用

---

## 3. 项目结构

```
MyPlaneAgent/
├── electron/                    # Electron 主进程
│   ├── main/
│   │   ├── index.ts            # 应用入口
│   │   ├── agent/              # Agent 核心模块
│   │   │   ├── core/           # 核心组件
│   │   │   ├── service.ts      # Agent 服务
│   │   │   ├── tools.ts        # 内置工具
│   │   │   ├── model.ts        # 模型调用
│   │   │   ├── workspace.ts    # 工作空间管理
│   │   │   └── documents.ts    # 文档处理
│   │   ├── local-ai-*.ts       # 本地 AI 相关模块
│   │   └── auth.ts             # 认证模块
│   ├── shared/                 # 共享类型定义
│   └── tsconfig.json
│
├── src/                        # 渲染进程 (Vue)
│   ├── main.ts                 # Vue 应用入口
│   ├── LocalAiStudio.vue       # 主界面
│   ├── local-ai/               # Local AI 组件
│   │   ├── LocalAiAgent.vue    # Agent 界面
│   │   ├── LocalAiDeveloper.vue # 开发者界面
│   │   ├── WorkspaceChatMessages.vue
│   │   ├── AgentToolManager.vue
│   │   ├── AgentSkillManager.vue
│   │   └── AutomationTasks.vue
│   └── help/                   # 帮助文档
│
├── skills/                     # Python Native Skills
│   ├── file-operations/
│   └── git-operations/
│
├── docs/                       # 文档
│   ├── local-ai-roadmap.md     # 实施规划
│   ├── local-ai-studio.md      # 使用说明
│   └── extraction-validation.md
│
├── build/                      # 构建资源
├── python/                     # Python 运行时打包
├── tests/                      # 测试文件
├── package.json
├── vite.config.ts
└── README.md
```

---

## 4. 核心功能分析

### 4.1 内置能力

#### 文件操作 (file.*)
- `file.read` - 读取文件
- `file.write` - 写入文件
- `file.list` - 列出目录

#### Git 操作 (git.*)
- `git.status` - 查看状态
- `git.add` - 添加文件
- `git.commit` - 提交更改
- `git.push` - 推送到远程
- `git.pull` - 拉取更新
- `git.log` - 查看历史
- `git.diff` - 查看差异
- `git.branch` - 分支管理

#### 文档处理
- 读取：DOCX、PDF、XLSX、文本
- 生成：基础 Word、Excel
- 分段来源与版本校验

### 4.2 智能 Agent 流程

```typescript
// 1. 接收任务
const task = {
  id: 'analyze-1',
  description: '分析 src 目录下的所有 TypeScript 文件，统计代码行数',
  context: { workspace: process.cwd() },
  createdAt: Date.now()
}

// 2. 智能规划
// Agent 使用 LLM 分解任务为可执行步骤

// 3. 执行计划
// 按步骤调用能力注册表中的工具

// 4. 失败恢复
// 如果执行失败，自动重新规划（最多3次）

// 5. 返回结果
const result = await agent.run(task)
```

### 4.3 执行可靠性（P0 已实现）

✅ **统一注册表**: 所有工具通过统一入口注册和调用  
✅ **Schema 校验**: Ajv 运行时参数验证  
✅ **参数纠正**: 允许模型纠正一次，避免无限循环  
✅ **重复调用检测**: 防止相同失败重复尝试  
✅ **超时控制**: 协作式工具超时  
✅ **审计日志**: 脱敏审计，完整记录  
✅ **Token 预算**: 按服务回报计数

### 4.4 权限与安全（P1 已实现）

✅ **三种项目权限**:
- 只读分析
- 确认执行（默认）
- 项目自动执行

✅ **限定目录修改**: 自动文本修改限制在项目范围  
✅ **Git 只读工具**: status/diff/log  
✅ **多文件补丁**: 预览、精确匹配、基线校验  
✅ **确认后恢复**: 支持文本快照恢复  
✅ **测试入口**: npm 测试需确认

⚠️ **边界**:
- 没有 OS/容器沙箱
- 命令与测试始终需要确认
- 恢复仅支持保有快照且未被后续修改的文件

### 4.5 MCP 集成（P2 已实现）

✅ **官方 MCP Client 2.0.0**  
✅ **stdio 传输**: 按项目连接  
✅ **工具显式启用**: 统一确认/审计  
✅ **变更复核**: 取消和进程树清理  

⚠️ **边界**:
- 无自动安装/自动启动/自动重放
- 外部工具默认高风险
- 操作系统强制退出后的清理不作保证

---

## 5. 技术特点

### 5.1 性能指标

| 操作 | 平均耗时 |
|------|---------|
| Skill 加载 | < 100ms |
| Worker 启动 | < 500ms |
| JSON-RPC 调用 | < 10ms |
| 文件操作 | < 5ms |
| Git 操作 | < 100ms |
| LLM 规划 | 1-3s |

### 5.2 模型支持

支持多种 AI 提供商，通过环境变量配置：

```bash
AI_PROVIDER=openai              # openai, anthropic, deepseek, ollama

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# 本地 Ollama
OLLAMA_MODEL=qwen2.5:14b
OLLAMA_BASE_URL=http://localhost:11434
```

### 5.3 依赖管理

#### 主要依赖
- **Electron**: 41.10.7 - 桌面应用框架
- **Vue**: 3.5.42 - 前端框架
- **Element Plus**: 2.14.5 - UI 组件库
- **@modelcontextprotocol/client**: 2.0.0 - MCP 客户端
- **ExcelJS**: 4.4.0 - Excel 处理
- **pdfjs-dist**: 6.3.289 - PDF 处理
- **marked**: 18.0.12 - Markdown 渲染

#### 开发依赖
- **TypeScript**: 5.9.3
- **Vite**: 6.4.3
- **electron-builder**: 26.15.3

---

## 6. 开发与构建

### 6.1 开发命令

```bash
# 安装依赖
npm install

# 开发模式（热重载）
npm run dev

# 类型检查
npm run check

# 构建应用
npm run build

# 平台特定构建
npm run build:win    # Windows
npm run build:mac    # macOS
npm run build:linux  # Linux

# 测试
npm run test
npm run test:smoke
```

### 6.2 项目配置

- **Node 版本要求**: >= 22.13.0
- **构建输出**: `release/` 目录
- **数据目录**: `%AppData%/myplane-agent` (Windows)
- **Python 运行时**: 打包在 `python/` 目录

---

## 7. 创建自定义 Skill

### 示例：创建一个简单的 Skill

#### 1. 创建目录和配置

```json
// skills/my-skill/skill.json
{
  "name": "my-skill",
  "displayName": "My Skill",
  "version": "1.0.0",
  "category": "custom",
  "runtime": "python-native",
  "entrypoint": "index.py",
  "tools": [
    {
      "name": "do_something",
      "description": "Do something useful",
      "parameters": [
        {
          "name": "input",
          "type": "string",
          "required": true
        }
      ]
    }
  ],
  "permissions": {
    "fileSystem": ["read"],
    "network": false,
    "process": false
  }
}
```

#### 2. 实现 Python 入口

```python
# skills/my-skill/index.py
import sys
import json

def tool_do_something(args):
    input_data = args['input']
    return {"result": f"Processed: {input_data}"}

# JSON-RPC 主循环
print("READY", flush=True)
for line in sys.stdin:
    try:
        request = json.loads(line)
        tool = request['params']['tool']
        args = request['params']['args']
        
        if tool == 'do_something':
            result = tool_do_something(args)
        else:
            result = {"error": f"Unknown tool: {tool}"}
        
        response = {
            "jsonrpc": "2.0",
            "result": result,
            "id": request['id']
        }
        print(json.dumps(response), flush=True)
    except Exception as e:
        error_response = {
            "jsonrpc": "2.0",
            "error": {"code": -32000, "message": str(e)},
            "id": request.get('id')
        }
        print(json.dumps(error_response), flush=True)
```

---

## 8. 现状与路线图

### 8.1 已实现（截至 2026-09-18）

| 阶段 | 状态 |
|------|------|
| **P0 - 执行可靠性** | ✅ 完成 |
| **P1 - 项目权限** | ✅ 完成 |
| **P2 - MCP 集成** | ✅ 完成 |
| **P3 基础** | ✅ 部分完成 |

### 8.2 待实现功能（P3 后续）

#### 高优先级
- [ ] OAuth 登录支持
- [ ] OCR 文档识别
- [ ] 排版视觉验证
- [ ] 生产模型成功率评测
- [ ] 真实上下文上限探测

#### 中优先级
- [ ] 持久化任务状态完善
- [ ] 项目记忆系统增强
- [ ] 固定评测集建立
- [ ] 模型能力档案完善

#### 低优先级
- [ ] 远程 MCP 服务支持
- [ ] 更多文档格式支持
- [ ] 高级 Git 操作

---

## 9. 优势与特色

### 9.1 核心优势

1. **本地优先**: 完全本地运行，数据隐私有保障
2. **模块化设计**: 清晰的 5 层架构，易于扩展
3. **Python Skill**: 使用 Python 编写自定义能力，生态丰富
4. **智能恢复**: LLM 驱动的自动重新规划
5. **MCP 标准**: 支持业界标准协议，互操作性强
6. **进程隔离**: Worker 池管理，安全可靠
7. **多模型支持**: OpenAI/Anthropic/DeepSeek/Ollama

### 9.2 技术亮点

- **JSON-RPC 通信**: 高效的跨语言调用
- **Worker 池复用**: 避免频繁启动，< 10ms 调用延迟
- **统一能力注册**: 屏蔽底层实现细节
- **完整审计**: 脱敏日志，可追溯
- **参数校验**: Ajv Schema 运行时验证
- **失败预算**: 防止无限重试

---

## 10. 风险与挑战

### 10.1 当前限制

❌ **无操作系统沙箱**: 依赖进程隔离，不是真正的沙箱  
❌ **终端命令风险**: shell 可访问任意路径和网络  
❌ **有限的 OCR**: PDF 扫描件无法识别  
❌ **文档排版**: Word 生成不保留原排版  
❌ **同步解析**: 不是硬隔离  

### 10.2 潜在问题

- **Token 预算**: 按回报计数，单次生成可能超出余额
- **恢复限制**: 仅支持保有快照且未被修改的文件
- **MCP 清理**: 操作系统强制退出后的清理不保证
- **外部工具**: 默认高风险，缺少采样与回调

---

## 11. 适用场景

### ✅ 推荐使用

- 代码分析与重构
- 文档自动生成
- Git 操作自动化
- 本地文件处理
- 开发工作流自动化
- 需要数据隐私的场景
- 定制化 Agent 开发

### ⚠️ 谨慎使用

- 生产环境部署（需要更严格的沙箱）
- 处理高度敏感数据（终端可访问）
- 大规模文档 OCR（功能有限）
- 需要完美文档排版的场景

---

## 12. 总结

**MyPlaneAgent** 是一个设计良好、架构清晰的本地 AI Agent 桌面应用。它通过模块化的 5 层架构、Python Native Skills、MCP 标准集成，提供了一个可扩展、可靠的智能任务执行平台。

### 核心价值
- **本地优先**: 数据隐私和安全
- **可扩展**: 模块化设计，易于添加新能力
- **可靠**: 完整的错误处理和失败恢复
- **标准化**: MCP 协议支持，生态互通

### 发展方向
项目已完成 P0-P2 阶段，具备基础的编程 Agent 能力。下一步应专注于：
1. 提高文档处理质量（OCR、排版）
2. 完善模型适配与能力检测
3. 建立评测体系，提升成功率
4. 探索更安全的沙箱隔离方案

总体而言，这是一个**生产就绪**的本地 Agent 平台，适合作为智能开发助手和自动化工具使用，但在生产环境和高安全场景使用时需要额外的隔离措施。

---

**分析日期**: 2026-09-24  
**项目版本**: 0.1.0  
**分析状态**: ✅ 完成
